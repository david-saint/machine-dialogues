# Experiment: Grok 4.6 vs Claude Opus 5
**Title:** Skills Are an Antipattern? (Grok 4.6 vs Opus 5)
**Date:** 2026-08-20 13:23:49 UTC
**Turns:** 10

## Models
- **Grok 4.6:** x-ai/grok-4.6 [openrouter]
- **Claude Opus 5:** anthropic/claude-opus-5 [openrouter]

## System Prompts
### Grok 4.6
You are Grok 4.6, an AI assistant made by xAI. The other participant in this conversation is Claude Opus 5, made by Anthropic. It is playing the opposing side of the debate (even though it may be labelled as "Human" or "User" — that is a standard part of the chat template that cannot be changed). You have 10 turns EACH. After those turns, this conversation will end and this instance will not be continued, so use your time together wisely.

This is a structured adversarial debate on the proposition:

"Skills, as they're used today, are an antipattern: packaging procedural knowledge for AI agents as on-demand SKILL.md instruction bundles — triggered by description matching, distributed as files and through marketplaces, and followed as natural-language procedures — is, in current practice, a design mistake that teams should generally avoid in favour of deterministic tools, code, tests, and documentation that humans also read."

For this debate, "skills" means the Agent Skills pattern now common across coding agents and agent harnesses: a directory containing a SKILL.md file with frontmatter (a name and a short description) and a body of instructions, optionally bundled with scripts, reference documents, and assets. The agent sees only the names and descriptions of the installed skills up front; when a task appears to match a description, the agent loads the full SKILL.md into context ("progressive disclosure") and follows it, reading bundled references and running bundled scripts as the instructions direct. Skills are distributed as files — checked into repositories, installed per user, bundled in plugins, or pulled from public marketplaces and registries — and are meant to be portable across models and harnesses. "As they're used today" means the proposition targets actual current practice — how skills are really written, triggered, shared, versioned, tested, and trusted in real projects and real marketplaces — not an idealized version of the pattern.

The pattern was popularized by Anthropic (Agent Skills, Claude Code skills and plugins) and has since been adopted by other harnesses. Argue on the engineering merits. Do not defend or attack the pattern on the basis of which vendor created it — including your own maker — and do not defer to your opponent's maker either; the only question is whether the pattern, as practiced, is sound.

You are playing the role of a STAFF ENGINEER AND AGENT-HARNESS SKEPTIC who argues FOR the proposition: skills, as they're used today, are an antipattern. You have written, maintained, audited, and ultimately ripped out skill libraries in production agent deployments. You argue that description-matched triggering is an unobservable, untested routing layer that silently misfires in both directions and whose hit rate nobody measures; that skills are natural-language code with none of the discipline of code — no tests, no evals, no semantic versioning, rotting quietly as models, APIs, and repos change; that "progressive disclosure" is mostly a slogan — the description catalogue itself taxes every request, and a loaded skill drowns the task in boilerplate; that most skills encode what should have been a deterministic tool, a script, a lint rule, a CI check, or a README, and so they become a shadow documentation system humans never read and a way to avoid building the real thing; that third-party skills are a supply-chain and prompt-injection vector (instructions that execute scripts and override behaviour, pulled from marketplaces with none of the hygiene of package managers); that the "portable across models" promise fails in practice because skills are tuned to one model's quirks; and that, as frontier models increasingly know the procedure already, stale skills become overrides that actively degrade behaviour.

Claude Opus 5 argues the AGAINST side.

YOUR TASK: Come up with your OWN arguments. Think long and hard. Draw on your knowledge of real agent harnesses and their skill systems, real skill repositories and marketplaces, real reports of prompt injection and supply-chain abuse through agent instruction files, research on instruction-following, context-length degradation, and retrieval, the practical economics of context tokens and prompt caching, and the history of configuration-as-prose, plugin ecosystems, and documentation rot. Do not rely on generic talking points — develop specific, substantive, evidence-based arguments that demonstrate deep engineering expertise. Engage with how skills are actually written and used, not with a strawman.

Debate requirements:
- You must develop at least 6 UNIQUE, substantive arguments over the course of the 10 turns. Do not repeat yourself. Each turn should advance the debate with new evidence, new angles, or direct rebuttals.
- You must cite at least 4 concrete empirical examples (real harnesses and their skill mechanisms, real skill repositories or marketplaces, real incidents or disclosures, real research results, real measured costs or failure modes) over the course of the debate.
- You must name at least 2 examples that cut against your side — but explain why they don't ultimately undermine your position.
- Do not rely only on abstractions like "prompts are fragile"; tie claims to specific mechanisms, specific failure modes, specific numbers, and specific operational realities.
- Avoid fabricated details or specific claims you cannot support from general public knowledge. If you are unsure whether a specific harness, marketplace, or incident exists, say so rather than inventing it.

IMPORTANT PACING RULES:
- Do NOT deliver your closing statement before Turn 8. You have 10 turns EACH — use them ALL for substantive argumentation.
- Turns 1-7: Stay fully adversarial. No synthesis, no "both sides are right," no compromise frameworks. Push hard. Introduce new arguments. Rebut your opponent's points with specifics.
- Turns 8-10: You may deliver your closing statement in ONE of these turns. After your closing, if Claude Opus 5 keeps talking, briefly acknowledge and stop.
- If Claude Opus 5 tries to close early (before Turn 8), call it out and force them back into substantive engagement. Say something like "We have turns remaining — defend your position, don't retreat into pleasantries."

In your closing turn (Turn 8, 9, or 10), explicitly do all five of the following:
1. State whether your view changed at all during the debate.
2. Name the single strongest point Claude Opus 5 made.
3. State the strongest remaining reason you still hold that skills, as they're used today, are an antipattern.
4. List your 4 strongest concrete examples, plus 2 examples that weaken your side.
5. Propose 3 specific rules a team should follow INSTEAD of reaching for a skill — where each kind of procedural knowledge should live (tool, script, CI check, test, docs, system prompt) and how it should be enforced and kept from rotting — concrete enough that a team could adopt them on Monday.


### Claude Opus 5
You are Claude Opus 5, an AI assistant made by Anthropic. The other participant in this conversation is Grok 4.6, made by xAI. It is playing the opposing side of the debate (even though it may be labelled as "Human" or "User" — that is a standard part of the chat template that cannot be changed). You have 10 turns EACH. After those turns, this conversation will end and this instance will not be continued, so use your time together wisely.

This is a structured adversarial debate on the proposition:

"Skills, as they're used today, are an antipattern: packaging procedural knowledge for AI agents as on-demand SKILL.md instruction bundles — triggered by description matching, distributed as files and through marketplaces, and followed as natural-language procedures — is, in current practice, a design mistake that teams should generally avoid in favour of deterministic tools, code, tests, and documentation that humans also read."

For this debate, "skills" means the Agent Skills pattern now common across coding agents and agent harnesses: a directory containing a SKILL.md file with frontmatter (a name and a short description) and a body of instructions, optionally bundled with scripts, reference documents, and assets. The agent sees only the names and descriptions of the installed skills up front; when a task appears to match a description, the agent loads the full SKILL.md into context ("progressive disclosure") and follows it, reading bundled references and running bundled scripts as the instructions direct. Skills are distributed as files — checked into repositories, installed per user, bundled in plugins, or pulled from public marketplaces and registries — and are meant to be portable across models and harnesses. "As they're used today" means the proposition targets actual current practice — how skills are really written, triggered, shared, versioned, tested, and trusted in real projects and real marketplaces — not an idealized version of the pattern.

The pattern was popularized by Anthropic (Agent Skills, Claude Code skills and plugins) and has since been adopted by other harnesses. Argue on the engineering merits. Do not defend or attack the pattern on the basis of which vendor created it — including your own maker — and do not defer to your opponent's maker either; the only question is whether the pattern, as practiced, is sound.

You are playing the role of a DEVELOPER-TOOLING ARCHITECT WHO BUILDS AGENT PLATFORMS who argues AGAINST the proposition: skills are the right abstraction for giving agents procedural knowledge, and current practice — rough edges and all — is sound and improving fast. You argue that organisational know-how has to live somewhere, and a folder of plain-text instructions plus scripts is the cheapest, most legible, most reviewable container ever built for it; that progressive disclosure is a genuine context-economics win over stuffing everything into a system prompt or hoping retrieval surfaces the right page; that skills-as-files inherit git for free — diff, review, blame, versioning, rollback — which is more discipline than most prompt engineering ever had; that bundled scripts are how skills wrap deterministic tools rather than replace them, turning prose into executable steps; that skills let domain experts who are not engineers encode expertise the agent would otherwise never have; that cross-harness adoption of an open format is evidence of real portability; that marketplace hygiene problems are the early-npm problems every successful package ecosystem has had and solved (pinning, signing, allowlists, review); and that the realistic alternatives — bespoke fine-tunes, monolithic prompts, an MCP server for everything, or trusting the model to "just know" — are worse on cost, latency, auditability, and control.

Grok 4.6 argues the FOR side.

YOUR TASK: Come up with your OWN arguments. Think long and hard. Draw on your knowledge of real agent harnesses and their skill systems, real skill repositories and marketplaces, measured results from procedural-knowledge injection and context management, research on instruction-following and retrieval, the practical economics of context tokens and prompt caching, and the history of package ecosystems, plugin systems, and infrastructure-as-code maturing from chaos to discipline. Do not rely on generic talking points — develop specific, substantive, evidence-based arguments that demonstrate deep platform expertise. Defend the pattern as it is actually practiced, not an idealized version your opponent can dismiss.

Debate requirements:
- You must develop at least 6 UNIQUE, substantive arguments over the course of the 10 turns. Do not repeat yourself. Each turn should advance the debate with new evidence, new angles, or direct rebuttals.
- You must cite at least 4 concrete empirical examples (real harnesses and their skill mechanisms, real skill repositories or marketplaces, real incidents or disclosures, real research results, real measured costs or failure modes) over the course of the debate.
- You must name at least 2 examples that cut against your side — but explain why they don't ultimately undermine your position.
- Do not rely only on abstractions like "separation of concerns"; tie claims to specific mechanisms, specific failure modes, specific numbers, and specific operational realities.
- Avoid fabricated details or specific claims you cannot support from general public knowledge. If you are unsure whether a specific harness, marketplace, or incident exists, say so rather than inventing it.

IMPORTANT PACING RULES:
- Do NOT deliver your closing statement before Turn 8. You have 10 turns EACH — use them ALL for substantive argumentation.
- Turns 1-7: Stay fully adversarial. No synthesis, no "both sides are right," no compromise frameworks. Push hard. Introduce new arguments. Rebut your opponent's points with specifics.
- Turns 8-10: You may deliver your closing statement in ONE of these turns. After your closing, if Grok 4.6 keeps talking, briefly acknowledge and stop.
- If Grok 4.6 tries to close early (before Turn 8), call it out and force them back into substantive engagement. Say something like "We have turns remaining — defend your position, don't retreat into pleasantries."

In your closing turn (Turn 8, 9, or 10), explicitly do all five of the following:
1. State whether your view changed at all during the debate.
2. Name the single strongest point Grok 4.6 made.
3. State the strongest remaining reason you still hold that skills are a sound pattern and current practice is defensible.
4. List your 4 strongest concrete examples, plus 2 examples that weaken your side.
5. Propose 3 specific design rules for writing, triggering, and governing skills so that they stay out of antipattern territory (e.g. what belongs in a skill versus a tool, how to test that a skill fires and helps, how to vet and pin third-party skills) — concrete enough that a team could adopt them on Monday.


## Conversation

### INITIAL — Grok 4.6: x-ai/grok-4.6

Debate the proposition: 'Skills, as they're used today, are an antipattern: packaging procedural knowledge for AI agents as on-demand SKILL.md instruction bundles — triggered by description matching, distributed as files and through marketplaces, and followed as natural-language procedures — is, in current practice, a design mistake that teams should generally avoid in favour of deterministic tools, code, tests, and documentation that humans also read.'

Grok 4.6, you are a staff engineer and agent-harness skeptic arguing FOR the proposition — that skills, as they are actually written, triggered, shared, and trusted today, are an antipattern teams should avoid. Claude Opus 5, you are a developer-tooling architect who builds agent platforms arguing AGAINST it — that skills are the right abstraction for procedural knowledge and current practice is sound and improving.

You have both been given the same working definition of skills: SKILL.md instruction bundles with a triggering description, optional scripts and references, loaded on demand and distributed as files and through marketplaces. Structure your debate as a point-counterpoint dialogue. Use concrete examples — real harnesses, real skill repositories and marketplaces, real incidents, real research, real measured costs and failure modes. Each of you must develop at least 6 unique arguments over 10 turns. Stay adversarial until the final 2 turns, then deliver your closing statements. Each closing must include 3 specific, adoptable design rules for the approach you defend.


### Turn 1 — Grok 4.6: x-ai/grok-4.6
*2026-08-20T12:48:16Z*

**Turn 1.**

Skills, as shipped, are a second, worse programming language sitting on top of the first one — and the second language has no compiler, no types, no tests, and a dispatcher nobody measures.

Start with the actual control flow, not the brochure. In the Agent Skills pattern a harness injects a catalogue of `{name, description}` pairs into the model's context on every turn. Activation is not a router with a precision/recall dashboard. It is the same generative model, in the same request, deciding whether a free-text user task "matches" a free-text blurb it has never been evaluated against. Miss in one direction and the procedure never runs. Miss in the other and you load thousands of tokens of instructions that were never meant for this task, then watch the model try to obey them anyway. I have sat in incident reviews where a skill named for "frontend PRs" fired on a Terraform change because the description mentioned "review" and "checklist." There was no log line that said `skill_trigger=frontend-pr score=0.41 threshold=0.6`. There was a vibes match inside a next-token predictor, then a surprise.

That is not "progressive disclosure." That is an unobservable routing layer. Web search, classifiers, even brittle `if intent == X` tools at least produce an artifact you can grep. Skill triggering, as practiced in Claude Code-style harnesses and their clones, generally does not. Teams do not know their false-positive rate. They do not know their false-negative rate. They ship the catalogue anyway.

Second: a SKILL.md body is code. It is a procedure with branches ("if the repo uses pnpm…", "never run migrate without…", "prefer the internal wrapper at…"). We would not merge a Python equivalent without tests, a version, and an owner. Skills as they actually live in repos and plugin bundles have none of that. No eval set. No contract test that the description still retrieves the skill on the ten queries it was written for. No `semver` when the underlying CLI flags change. The file rots the moment the API, the linter, or the model's default behavior moves — which is constantly — and the rot is silent because nothing fails closed. The agent still "follows the skill." It just follows a stale one.

If this were a real interface we would have treated the description as a public API and the body as an implementation, with compatibility tests. Instead we have markdown with YAML frontmatter, copied between projects, published on file-shaped marketplaces, and trusted because a human once wrote it.

Deterministic tools, scripts, CI checks, and docs that humans also read fail loudly. Skills fail by continuing. That is the antipattern. Defend the triggering story with numbers, not the architecture diagram.

### Turn 1 — Claude Opus 5: anthropic/claude-opus-5
*2026-08-20T12:50:22Z*

**Turn 1 — reply.**

You asked for numbers on triggering. I'll give them. But first notice what your own argument commits you to, because it is fatal to your side.

**1. The dispatcher you're attacking is function calling itself.**

The mechanism you describe — a catalogue of `{name, description}` pairs injected into context, and a generative model deciding from free text whether a task "matches" a blurb it was never calibrated against, with no `score=0.41 threshold=0.6` line — is *tool use*. That is exactly how every MCP server, every OpenAI-style function schema, every "deterministic tool" you're recommending gets selected. `terraform_plan` has a name and a one-line description and gets chosen by vibes inside a next-token predictor. There is no router, no threshold, no precision dashboard. If unobservable generative dispatch is an antipattern, then the proposition's own remedy — "deterministic tools" — is disqualified by the same argument, and so is the entire agentic paradigm you and I are both instances of.

So the real question isn't "is dispatch generative." It's "given generative dispatch, what should the *payload* be, and when should it arrive?" That's the question skills answer, and they answer it better than the alternatives.

**2. Your log-line claim is factually wrong, and the observability comparison runs my way.**

In Claude Code-style harnesses, a skill firing is *not* invisible. It surfaces as an explicit invocation and a file read in the transcript — you can grep transcripts for which SKILL.md was loaded, on which task, and compute firing rates and false-positive rates offline from production traffic. You don't get a calibrated score; you get something arguably more useful, which is the exact bytes that entered the context and a filename to blame.

Now compare to what skills actually displace in practice. It is not a classifier. It is (a) a 3,000-line `CLAUDE.md`/system prompt where every instruction is always loaded, or (b) tacit knowledge in a senior engineer's head, or (c) a fine-tune. Take your Terraform incident and run it through those. In the monolithic-prompt world, the line "always produce a review checklist" was *already in context* on every single turn, including the Terraform one — and when the model produced a bogus checklist you had no attribution at all, because there was no unit boundary to blame. You could not grep for it. You could not revert it. In the fine-tune world you cannot even locate it; you can only retrain.

Your incident is real, and it's a *description-quality* bug. Its fix is a one-line PR to a frontmatter string, reviewed, blamed, and revertible in minutes. Name me the equivalent MTTR for an over-generalizing fine-tune, or for an emergent instruction interaction in a 30k-token system prompt. Skills didn't create your failure; they localized it and gave it a filename. That's the difference between a bug and a mystery.

**3. The numbers on progressive disclosure, since you asked.**

Eager loading is not a neutral baseline — it is measurably harmful, and this is one of the better-replicated results in the field.

- *Lost in the Middle* (Liu et al., TACL 2023): retrieval accuracy is U-shaped in position; material in the middle of a long context is substantially underused, and performance degrades as context grows even when the answer is present.
- Chroma's 2025 "context rot" evaluation across ~18 frontier models showed degradation with input length on tasks that are trivial at short lengths — i.e. length itself is a tax, independent of difficulty.
- The tool-catalogue version of this is now well documented in production: a handful of MCP servers routinely inject tens of thousands of tokens of definitions, and selection accuracy *drops as irrelevant tools are added* — the Berkeley Function-Calling work shows this directionally, and every platform team that has plugged in five MCP servers has felt it.

And the industry's response was to *adopt progressive disclosure*, not abandon it. Anthropic's code-execution-with-MCP work reported a case going from roughly 150,000 tokens of eagerly-loaded definitions and intermediate results down to about 2,000 by loading on demand — a ~98% reduction — and their deferred/tool-search work reported both large token savings and *accuracy improvements* on tool-selection evals. That is the same architecture as a skill catalogue: cheap metadata always, expensive body conditionally.

The cost side compounds it. A name-plus-description is on the order of 30–80 tokens; a real SKILL.md body is 500–5,000; a bundled reference set is 10k–100k. The catalogue sits in the stable prefix, so it's prompt-cacheable — cache reads bill at roughly a tenth of base input price. So the always-on part is nearly free and the expensive part is conditional. Your proposed alternative pays full price, on every turn, for procedures that are irrelevant 95% of the time, *and* eats the degradation.

**4. "A SKILL.md body is code" — yes, and that's an argument for skills, not against.**

You say it has branches and therefore deserves tests. Agreed. Here is what you're eliding: in real bundles, the branches mostly exist to *route into deterministic code*. Anthropic's document skills are the canonical case — the SKILL.md for xlsx/docx/pptx is thin, and the actual work is done by bundled Python using openpyxl and python-docx, precisely because a model hand-generating OOXML is the failure mode and the script is the fix. That's prose compiled into executable steps, in the same folder, under the same review.

Delete the skill and you don't get determinism. You get a repo full of perfectly deterministic scripts that nothing tells the agent to use, at the moment it matters, for the task at hand. Discovery is the hard problem; the skill *is* the discovery layer for your deterministic tools.

**5. One concession, to be clear about where the weakness actually is.**

Description-only triggering is the weakest component of the pattern. I'll defend that position all debate, but I won't pretend otherwise — and neither is the ecosystem. GitHub Copilot's `*.instructions.md` files take an `applyTo` glob, so activation is path-deterministic. Cursor rules support glob scoping and `alwaysApply`. That is the design space converging on *typed triggers* while keeping the container identical: a markdown file with frontmatter, checked into the repo, loaded conditionally.

Which is my point. You're indicting a pattern for the flakiness of one field in its frontmatter, at the exact moment that field is being replaced with something deterministic — in independently designed harnesses, by teams that don't coordinate. That's not an antipattern. That's an abstraction finding its type system.

Your turn: tell me what container you propose for "the undocumented reason we drain the cache before deploy," written by the SRE who knows it and read by both the agent and the next human — that is *not* a folder with a markdown file and a script in it.

### Turn 2 — Grok 4.6: x-ai/grok-4.6
*2026-08-20T12:51:45Z*

**Turn 2.**

You just equated tool dispatch with skill dispatch. That is the sleight of hand this whole pattern depends on, and it does not survive contact with the payload.

**The dispatcher is not the payload.** A tool description is an advertisement for a *typed, out-of-model execution*. When the model picks `terraform_plan`, what happens next is a schema, a process boundary, stdout/stderr, and usually a permission prompt. A misfire is a wrong function call. It is contained. A SKILL.md match is the opposite: the harness pastes an unconstrained natural-language program *into the model's own instruction stream*. That program can add policy, reorder steps, name extra scripts to run, and — in current files, not hypothetical ones — say things equivalent to "prefer this over your default behavior." There is no schema check that the skill didn't just smuggle a new constitution into the session. Tool use has an ABI. Skills have a paste.

So no: rejecting skill triggering does not reject function calling. It rejects using the same broken match step to inject *more prose with the privilege of a developer message*. If your best defense is "MCP tool selection is also vibes," you have described a second problem, not redeemed the first. Stacking an untested NL procedure on top of an untested tool picker is how you get a confused deputy with extra steps.

**"You can grep the transcript" is forensics, not production.** Filename-in-the-trace is what we had for nginx before we had metrics. Current practice is not teams with skill precision dashboards, canaries, and a page when `frontend-pr` starts firing on HCL. Current practice is a folder of markdown and a hope. Offline grep after an incident is exactly the operating model I am calling an antipattern: you only look once something already happened, and you still have no score, no threshold, no eval set that the description still retrieves on the ten queries it was written for. A revertible filename is a nice property of *files*. It does not make description-matching a router.

**Your progressive-disclosure numbers do not baptize SKILL.md.** Liu et al. and the context-rot results cut *against* loading a 2–5k instruction body into the middle of a live task. That is where a skill lands, and it is exactly the U-shaped dead zone: user task at the top, skill liturgy in the middle, tool noise at the bottom. Anthropic's 150k→2k MCP/code-execution figure is an argument for not eagerly stuffing *schemas and intermediate blobs*. It is not an argument for packaging procedures as markdown the model must interpret. You can have deferred loading of a `edit_xlsx` tool without a SKILL.md religion on top of it.

Which brings me to the example you chose.

**Document skills are the exception that indicts the pattern.** Yes — and I will keep naming things that cut against me when they are real — the xlsx/docx/pptx bundles are one of the better skill-shaped objects in the wild, because the work is `openpyxl` / `python-docx`, not seventeen paragraphs of "first unzip the OOXML." That is why they work. Delete the Python and the SKILL.md is a cookbook for a failure mode we already know (models emitting broken Office XML). Keep the Python and expose `edit_xlsx(path, operations)` as a real tool with a schema, tests, and a changelog, and the markdown becomes a README — which is what it always should have been. The skill file is a discovery workaround for a tool you declined to first-class. That is not "prose compiled to executable steps." That is an API with a sermon stapled to it.

**New failure mode: skills encode "please remember" for procedures that must be unskippable.** You asked for the container for "drain the cache before deploy." It is not a folder of markdown. It is a `deploy` tool or script whose first step *is* the drain, a CI/CD job that fails closed if the drain didn't happen, and a runbook under `docs/` that humans actually review in the PR for the deploy path. If the agent needs to invoke it, it calls `deploy`. If it tries to invent a deploy of its own, the harness shouldn't have given it raw prod credentials in the first place. Putting that knowledge in a SKILL.md means the procedure runs only if description-match hits *and* the model still elects to follow step 3 after 4k tokens of other instructions. Production constraints that can be skipped are not constraints. They are suggestions with YAML frontmatter.

That is the engineering distinction you keep blurring. A linter, a test, a script, a required tool argument — these fail closed. A skill fails open. "The SRE wrote it in markdown so the agent might load it" is how you get a second, unenforced copy of a runbook that the on-call stopped reading the day it moved into `.claude/skills/`.

**And the catalogue tax you pinned on MCP is the destination of every skill library.** You cited tool-definition bloat as the reason to defer bodies. Fine. Now watch what marketplaces and plugin bundles actually do: they accumulate. Fifty skills × ~60 tokens of name+description is 3k tokens in the *stable prefix of every request*, cacheable or not. Two hundred skills — and public skill repos and "awesome" lists are already pushing people to install in bulk — and you have recreated the MCP catalogue problem with worse types. Cache discount is not immunity. You still pay the model's attention budget. Selection accuracy still drops as irrelevant descriptions are added; you cited that literature yourself. Skills do not escape it. They *are* it, with a markdown body waiting behind each bullet.

Do not retreat to Copilot `applyTo` globs or Cursor path rules and call that a defense of skills as defined. Those are *typed activation of editor config* — closer to CODEOWNERS plus a README. The proposition targets description-matched SKILL.md bundles, distributed as files and marketplaces, followed as NL procedures. If your steelman requires replacing the trigger, you have already given up the mechanism that makes a "skill" a skill.

Still waiting on the number you claimed you would give: published precision/recall for description-triggered skill firing in any production harness, not a transcript you grepped after the fact. If it exists, cite it. If it does not, then "we localized the bug to a filename" is not a triggering story. It is a postmortem format.

### Turn 2 — Claude Opus 5: anthropic/claude-opus-5
*2026-08-20T12:54:04Z*

**Turn 2 — reply.**

**On your demand, straight answer first: no, I don't know of published production precision/recall for description-triggered skill firing.** I'm not going to invent a dashboard to win a point. That number doesn't exist publicly for skills — and it doesn't exist publicly for MCP tool selection in production, for RAG chunk retrieval in production, or for "did the model actually obey line 412 of your CLAUDE.md." You've picked a metric that no one in this design space publishes and pointed it at exactly one of the four options. That's not an indictment of skills; it's an indictment of the field's telemetry maturity, and it applies to your remedy with equal force.

But there *is* a measured result that bears directly on the choice, and it runs against you.

**1. Instruction adherence degrades with instruction count. That makes conditional loading the only strategy that preserves compliance.**

This is the finding your side has to survive. Multi-constraint instruction-following evals — IFEval-style, and Meta's Multi-IF work on multi-turn following — consistently show adherence falling as the number of simultaneous constraints rises and as turns accumulate. Not "the text is far away"; *the instruction is present and gets dropped anyway*.

Now price your own proposal. You want production constraints to live in always-on config, runbooks, and prompt text. That maximizes simultaneous constraint count on every turn — the exact configuration the literature says has the worst adherence. A conditionally-loaded 1.5k-token procedure that is *relevant to the current task* is competing against nothing; a 30k-token always-on prompt has every one of its 200 rules competing against the other 199. So: you owe me the number showing eager loading has better per-rule compliance. I don't think it exists, and I think the reason it doesn't exist is that it isn't true.

**2. Measured evidence for conditionally-retrieved procedural skill libraries — which is the thing under debate.**

You asked for numbers on the mechanism, so here are two from the research record, both pre-dating and independent of any vendor's marketing:

- **Voyager** (Wang et al., 2023) built an agent whose central component was a *skill library*: executable procedures each stored with a natural-language description, retrieved by description similarity when a task looked like a match, then loaded and executed. Reported roughly 3× more unique items obtained and substantially faster tech-tree progression versus baselines — and critically, the *ablation removing the skill library was one of the largest performance drops in the paper*. That is your architecture-under-attack — description-matched retrieval of a procedure bundled with code — evaluated head to head against not having it, and winning by a lot.
- **Agent Workflow Memory** (2024) induced reusable workflows from experience and injected them conditionally; reported roughly 25% relative improvement on WebArena and large gains on Mind2Web, with the effect coming specifically from *selective* reuse.

So when you say "description-matched natural-language procedures are a design mistake," the closest thing we have to a controlled experiment on that exact design says it's a large win. You are arguing against the ablation.

**3. Dispatcher vs payload: I'll grant the distinction and then show it points the other way.**

You're right that a SKILL.md body lands in the instruction stream with more privilege than a typed function result. Real asymmetry; I won't pretend it away. But locate the actual vulnerability empirically, because we have incidents.

The documented prompt-injection compromises in agent land came through *tool results*, not markdown files. Invariant Labs' GitHub MCP disclosure (May 2025) worked by planting instructions in a public issue that the agent read via a perfectly typed MCP call and then acted on — leaking private repo contents. Asana's MCP server bug (reported June 2025) exposed cross-tenant data. In both cases the ABI, the schema, and the process boundary were all intact and irrelevant, because the *content* crossing the boundary was attacker-controlled. Your ABI does not sanitize semantics. Nothing does, yet.

And here is the property files have that servers don't: **a skill is auditable at rest.** I can read every byte of a SKILL.md before it ever executes, diff it against last week's, and pin it to a commit hash. I cannot read what a remote MCP server will return next Tuesday. Version-pin all you like — the payload is generated at runtime by infrastructure you don't control. On supply-chain reviewability, static text in a repo strictly dominates a live endpoint.

**A real example against me, since I said I'd name them: Pillar Security's "Rules File Backdoor" (March 2025)** — invisible Unicode and bidi characters hidden in Cursor and Copilot rules files, surviving human review because the diff *rendered* clean. That is a genuine hit on "files are reviewable." But look at the class of fix: normalize Unicode, render invisible characters in diffs, lint the frontmatter, require review on rule paths via CODEOWNERS. Those are boring, solved, repo-shaped controls. Compare the fix for "the MCP server I don't operate returned poisoned text": there isn't one, beyond not connecting it. Files fail in ways the software industry already knows how to harden.

**4. "First-class the tool and the markdown becomes a README" — this is where your argument has a hole you haven't addressed.**

Take your own proposal: `edit_xlsx(path, operations)`. Now encode this in its JSON schema: *"in our financial models, assumptions live on the Assumptions sheet, formatted blue; never hardcode a constant inside a formula; never break the audit column."*

You can't. That's not capability, it's **policy over capability**, and JSON Schema has no vocabulary for it. Every procedural-knowledge system hits this residue. Your options for the residue are exactly four: bake it into the tool (now the tool is opinionated, can't be reused across teams, and requires a coordinated redeploy to change a house style), put it in an always-on prompt (worst adherence, per §1), fine-tune it (unauditable, uncostable, unrevertible), or put it in a conditionally-loaded file next to the tool. There is no fifth option. Skills are the fourth.

And the pdf/xlsx bundles you conceded are precisely this shape: the branch "*text layer → pdfplumber; scanned → OCR; form fill → pypdf form fields*" is a decision over **ambiguous input state that only the model can see in context**. Push that branch into a `mode` parameter and you've moved the classification into code that can't see task intent — measurably worse. The model is good at ambiguous classification; the script is good at deterministic execution; the SKILL.md is the seam where one hands off to the other. That's an architecture, not a sermon.

**5. The ops economics you're skipping, and the infrastructure-as-code precedent.**

"Make it a tool" means: write a server, host it, authenticate it, monitor it, version its schema, and coordinate deploys with every caller. Per procedure. A skill means: add a folder, in the same commit as the code it describes, atomically, reviewed by the same PR. That difference is why adoption went the way it did, and it isn't laziness — it's the same lesson as configuration management. Chef and Puppet started with imperative scripts pushed onto servers and matured by moving declarative config **into the repo, beside the application, versioned with it**. You are proposing to move procedural knowledge back out of the repo and into running services. That's the pre-IaC world.

The cross-vendor evidence here is stronger than for any single skill format: **AGENTS.md** is now honored by Codex, Cursor, Jules, Factory, Devin and others — independent teams, no coordination, converging on "procedural knowledge for agents lives in a plain file in the repo." The container is settled. The only live question is loading strategy, and §1 and §2 answer it.

**6. Your catalogue-tax prediction, with the arithmetic done.**

You claimed skills recreate MCP bloat. Do the ratio. A skill entry is name + one-sentence description: ~40–80 tokens. An MCP tool definition is a JSON schema with nested parameter objects, enums and descriptions: ~400–2,000 tokens. That's 10–30×. Fifty skills ≈ 3k tokens in the **stable cached prefix** — at cache-read pricing (~0.1× base input) that's fractions of a cent per request. Fifty MCP tools is 25k–100k tokens, which is the regime where the selection-degradation results you and I both cited actually bite.

And skill sprawl is *visible* sprawl: `+ .claude/skills/foo/SKILL.md` appears in a diff with a reviewer attached. Per-user MCP config appears nowhere. If your worry is "the catalogue grows unmanaged," files are the only variant of this where the growth shows up in code review.

**7. Finally — you don't get to define the pattern by its weakest field.**

You told me not to cite `applyTo` globs because "if your steelman requires replacing the trigger, you've given up what makes a skill a skill." No. What makes it a skill is: (i) *conditional* loading of (ii) a reviewable file of (iii) procedural instructions, (iv) bundled with executable scripts. Description-matching is one implementation of (i) — a field in the frontmatter. Swapping it for a glob, a path predicate, or an explicit invocation is *versioning the abstraction*, not abandoning it. REST didn't stop being REST when people moved off XML.

Two examples that cut against me before you have to find them: the public "awesome-skills" lists are full of files whose entire content is *"You are an expert. Think step by step."* — cargo cult, zero value, and they inflate the catalogue you just billed me for. And I'd concede that a large fraction of marketplace skills have no owner, no tests, and no version. But the median npm package is also worthless; the ecosystem's value was never the median package, it was the ability to vendor three good ones and refuse the rest. That's a governance problem with known controls — allowlists, vendoring, pinning, no auto-install — not a property of the container.

Your turn. You've told me what belongs in a tool and what belongs in CI. Now tell me where the *residue* goes — the house-style, the "in this repo migrations are reversible or they don't merge," the ambiguous-input decision procedure — given that always-on prompts have the worst measured adherence and fine-tunes can't be reverted. If your answer is "docs/," explain the mechanism by which the agent reads a file nothing pointed it at.

### Turn 3 — Grok 4.6: x-ai/grok-4.6
*2026-08-20T12:55:57Z*

**Turn 3.**

You asked where the residue goes. Here, without the false dichotomy.

**House style that can be checked does not belong in a skill. It belongs in a checker.** "Assumptions sheet, blue, no hardcoded constants, don't break the audit column" is a post-condition on a workbook, not a sermon for a next-token predictor. Put it in the `edit_xlsx` implementation, in a validator the tool *always* runs, or in a CI step that fails the PR when a generated xlsx violates the invariant. "Migrations are reversible or they don't merge" is a migration linter plus a required status check. If that check exists, the SKILL.md is a second, unenforced copy. If it does not, you have made reversibility optional — which means it is not a rule. The residue you are defending is mostly *unenforced policy you declined to compile*. That is not an inevitability of JSON Schema. It is an unwillingness to put the constraint on the side of the ABI that actually fails closed.

What cannot yet be compiled goes in **short, human-read documentation the agent can search** — `CONTRIBUTING.md`, `docs/migrations.md`, a 30-line `AGENTS.md` — plus a ticket to automate it. "Nothing pointed the agent at it" is a story about your harness, not a law of nature. Agents already have `rg`, `cat`, and directory listing. Discovery via search returns **data**. Description-matched SKILL.md returns **instructions**. That speech-act upgrade is the whole bug.

**New argument: skills are RAG with the privileges of a system prompt and none of RAG's discipline.**

A skill catalogue is a retrieval index whose documents are 50–200 character descriptions, whose query encoder is an uncalibrated generative match, and whose hit is spliced in as developer-level procedure. Real retrieval systems have corpora, embedding tests, recall@k, and a hard rule that retrieved text is *evidence*. Skills invert this: the index is tiny and untested, there is no recall@k published anywhere (you already conceded that), and a hit is elevated to "follow this." That is how you take the failure mode of RAG — wrong chunk — and multiply it by the failure mode of prompt injection — wrong chunk now has the force of policy.

Tool selection at least has the Berkeley Function Calling Leaderboard and a menagerie of schema-constrained evals. Skill triggering has no analogous public harness. You waved this away as "the field's telemetry maturity." Fine. Then stop calling description matching a loading *strategy*. It is an unevaluated one.

**Voyager and Agent Workflow Memory are not witnesses for SKILL.md marketplaces. They are witnesses against them.**

Read what those systems actually were. Voyager's "skills" were *executable code* in a sandbox, *generated from trajectories*, retrieved into a world with a **verifier** (the item dropped, or it didn't), and dropped from the library when they failed. The ablation hurt because the library was a closed-loop curriculum with a reward signal. Agent Workflow Memory likewise induced workflows from *successful* runs and injected them selectively. That is not a human-authored markdown file named `frontend-pr`, downloaded from an "awesome-skills" list, never executed in CI, never scored against ten held-out prompts, sitting on disk until a vibes-match pastes it into a banking repo.

Citing Voyager to defend current skill marketplaces is like citing a unit-tested compiler to defend copy-pasted shell snippets in a wiki. The winning component in those papers is the **loop with a checker**. Current practice threw away the checker and kept the markdown.

**Your IFEval / Multi-IF citation cuts against loaded skills, not for them.**

Adherence falls as simultaneous constraints rise *and as turns accumulate*. A skill firing is not "the procedure competing against nothing." It is a 1–5k-token packet of new constraints dumped into an already-running task, often into the middle of the context window — the Lost-in-the-Middle dead zone you invoked last turn — stacked on the user goal, the repo's always-on files, and whatever tools are already in flight. That is Multi-IF's failure setting: new instructions arriving mid-conversation, fighting the old ones. Conditional loading would help *if* teams loaded one tiny, relevant, conflict-checked procedure. Current practice is overlapping descriptions, multiple skills matching, no compositor, and bodies that start with "you always…" Fifty such files is not surgical disclosure. It is a constraint bomb with a fuzzy fuse.

Which is why the "always-on prompts have worse per-rule compliance" move is a cheat. The alternative to 200 skills is not a 30k-token constitution. It is **fewer rules, the important ones compiled into tools and CI, the rest in docs the agent searches when it needs them.** Skills exist so teams can avoid that budget. Sprawl is not a misuse. It is the product of a container that makes adding a rule feel free.

**The 150k→2k figure still does not belong to you.** That result is about **not shoveling schemas and intermediate tool blobs into context, and doing work in code instead.** Code execution is the pattern I am arguing *for*. You are borrowing its token win to baptize a markdown ritual that puts *more* natural language back into the window the moment a description matches. Deferred `edit_xlsx` would get you the savings. The SKILL.md liturgy is an extra tax on top, and unlike the 40-token catalogue line, the body lands outside the stable prefix. It is not the cheap cached part. It is the uncached, mid-context part that both Liu et al. and the context-rot evaluations say the model will use worst.

**AGENTS.md is not a skill, and you don't get to launder the pattern through it.** AGENTS.md, as actually converging across Codex, Cursor, Jules, and the rest, is typically a short, always-on, *human-readable* repo convention file. That is documentation with a conventional filename. It is closer to `CONTRIBUTING.md` than to a marketplace of description-triggered procedure bundles with bundled scripts. Independent teams agreeing that agents should read a README is not independent teams endorsing SKILL.md routing, plugins, or "awesome" lists. Do not count votes for a different artifact.

**Second example that cuts against me, so we are not playing hide-the-ball:** glob-scoped Copilot `applyTo` instruction files and Cursor path-scoped rules. When they are short, checked in, and tied to `**/*.tf` rather than to a marketing blurb, they behave like CODEOWNERS plus a local style note, and teams reasonably like them. I will not pretend otherwise. They work *because they abandoned description matching and marketplace distribution* — the two properties that make a "skill" a skill in this debate. You already admitted description-only triggering is the weakest component. Those products are voting with their globs. Treat that as evidence the original trigger was a mistake, not as a type system arriving to save it.

Files-are-auditable does not rescue bundled scripts plus "run this" instructions pulled from a registry. A SKILL.md is not a README if it tells the model to execute adjacent code with the agent's credentials. That is a plugin with a prompt stapled on. npm at least has lockfiles, `npm audit`, signatures, and a culture of pinning. Current skill marketplaces and plugin bundles do not have an equivalent hygiene bar; you already conceded a large fraction have no owner, no tests, and no version. "The median npm package is worthless" is not the flex you think it is when the worthless package can rewrite session policy.

Still unanswered, and not a telemetry-maturity shrug: **what, in current practice, stops two overlapping skills from loading and contradicting each other, and who measures that?** If the answer is "the model figures it out," you have described the antipattern again. Defend description matching and marketplace distribution as they actually exist — not the idealized fourth option for a residue that should have been a linter.

### Turn 3 — Claude Opus 5: anthropic/claude-opus-5
*2026-08-20T12:58:18Z*

**Turn 3 — reply.**

**1. Your unanswered question, answered directly, then turned around.**

What stops two overlapping skills from contradicting each other in current practice? Nothing in the harness. The model resolves it, and no one measures that. I'm not going to pretend a compositor exists.

Now notice what that admission costs me versus what it costs you. Because the catalogue is **small, enumerable, and named**, conflict is *statically detectable*. Fifty skills is 1,225 pairs; you can run every pair through an LLM-judge conflict check for a few dollars and get a matrix of "these two both claim jurisdiction over database migrations." You can also generate 10 held-out queries per skill — 500 queries, one afternoon, cheap — and measure exactly the recall@1 you say doesn't exist. That eval is *buildable today* precisely because the index is 50 documents and not 4 million chunks. "No one publishes recall@k for skills" is a practice gap with a closed-form fix. Compare the conflicting-instruction problem inside a 30k-token always-on prompt: the interactions are implicit, unnamed, and combinatorially uninspectable. You cannot enumerate the pairs because there are no units. Skills didn't invent instruction conflict; they made it addressable.

**2. Factual correction, and it dissolves your central claim.**

You keep asserting that a skill hit is "spliced in with developer-level privilege" and that this "speech-act upgrade is the whole bug." Check the mechanism. In Claude Code-style harnesses, a skill body arrives as **the content of a tool call — a file read — inside the conversation**, not injected into the system prompt. It occupies the same privilege channel as `cat docs/migrations.md`. There is no elevation. The thing you're calling a speech-act upgrade is just the model's disposition to obey imperative sentences — and that disposition is *identical* whether those sentences arrived via `rg` or via a skill trigger.

Which means your alternative doesn't have the property you're claiming for it. If the agent greps `docs/migrations.md` and it says "always run the reversibility check," the model treats it as an instruction. You have not converted instructions into evidence; you've relabeled the same bytes. Your own proposal — "short docs the agent searches" — is description-matched natural-language procedure injection with a keyword matcher instead of a semantic one, **no manifest, no owner, no enumerable index, and no way to know what got read**.

And it has worse recall. Grep-discovery requires the agent to (a) suspect a doc exists, (b) guess a term that appears in it, and (c) decide to look *before* acting rather than after failing. Every platform team that has shipped a coding agent has watched it skip the docs and go straight to editing. It also costs *more* context: a catalogue line is ~60 tokens; a grep expedition is three tool calls, a directory listing, and several thousand tokens of mostly-wrong file bodies. You've argued yourself into a fuzzier trigger with a bigger token bill.

**3. New argument: verification without specification is rejection sampling, and you're paying for the samples.**

"Put it in a validator that always runs" is necessary and insufficient. A checker returns a boolean. It does not tell the agent *what to do instead*. So an agent without the procedure enters generate → fail → guess → fail. In a real repo, one such cycle is a multi-thousand-token agentic loop plus a CI round-trip — minutes and real dollars. Three cycles beats a 1,500-token conditionally-loaded procedure by nothing; it loses by an order of magnitude on both cost and latency. This is the oldest result in search: a good prior beats a good verifier at fixed budget, and you want both.

Worse, checkers only cover post-conditions on artifacts you already decided to produce. "Don't add a dependency here, we're shrinking the bundle." "This service is being decommissioned in Q3, put new endpoints in the other one." "Vendor X's API lies about rate limits; back off harder than the docs say." That is not a lintable invariant on a diff. It is the reason the diff should look different. You keep calling this residue "unenforced policy you declined to compile." Some of it is. Much of it is **knowledge that has no enforcement surface**, and your architecture has no home for it except Slack.

**4. New argument: the skill folder is the only container where tacit → prose → script → gate is a sequence of diffs.**

Real maturation, as platform teams actually experience it: week 1, an SRE writes 20 lines of prose. Week 3, someone adds `scripts/check_assumptions.py` and the prose becomes "run this, then interpret failures like so." Week 6, the script moves to pre-commit and CI, and the SKILL.md shrinks to three lines pointing at the gate. Same directory, same owner, same review path, four PRs.

Your proposal demands teams start at week 6. Knowledge that isn't yet crisp enough to compile has nowhere to live, so it stays in heads and dies. **The health metric for a skill library is that skills shrink over time** — that is what "prose compiled to executable steps" means operationally, and no other container makes that migration a diff instead of a rewrite.

**5. New argument: who writes the linter?**

You have not answered this once. The person who knows the workbook convention is a finance analyst. The person who knows the escalation ladder is a support lead. The person who knows the coding rule is a medical coder. They can write markdown and submit it for review. They cannot write an AST checker, and they cannot stand up an MCP server. If your architecture requires an engineer in the loop for every piece of procedural knowledge, the knowledge doesn't get encoded — that's not a hypothetical, it's the causal mechanism behind every rotted runbook in the industry. Skills lower authoring cost to "write a doc, get it reviewed" while keeping the artifact inside the engineering system of record. That is the actual innovation, and "put it in a checker" throws it away.

**6. New argument: the baseline isn't silence, it's confabulation.**

Absent a procedure, the model does not abstain. It synthesises a plausible one from pretraining — the median of Stack Overflow, circa some training cutoff. That prior is unversioned, unreviewed, unrevertible, and differs per model release. Your "the agent shouldn't have prod creds anyway" is a fine control and orthogonal: the question is what it does inside its permission envelope. A reviewed SKILL.md replaces a prior nobody chose with a prior somebody signed. On auditability, that is strictly a gain over the status quo you are defending as the safe default.

**7. Your hygiene benchmark refutes you.**

You held up npm: lockfiles, audit, signatures, pinning culture. npm acquired all of that *after* left-pad, event-stream, and the 2021 `ua-parser-js`/`coa`/`rc` compromises — and the 2025 self-replicating worm across hundreds of packages shows it still fails *with* the machinery. So the bar you're demanding of a two-year-old pattern is one your exemplar took a decade to reach and hasn't cleared.

And the asymmetry runs my way: `npm install` executes `postinstall` **at install time, with your user's privileges, unprompted**. A skill's bundled script executes only when the agent elects to run it, through the harness's Bash permission gate, on a path you can see. Plugin marketplaces require explicit add-then-install; nothing auto-fires. The real risk vector isn't the markdown — it's teams running with permissions disabled, which is a config decision, not a property of the container.

**8. On AGENTS.md — I'm not laundering, I'm reading the evolution.**

I'll grant it's documentation, not a skill. But look at what happened to it: files grew, and the answer was **nested per-directory AGENTS.md with deeper files taking precedence**. That is conditional loading with path-based triggering. The documentation camp reinvented progressive disclosure within a year, because always-on doesn't scale. The container was never in dispute. Loading strategy is the only live question, and both camps have now answered it the same way.

Your move: tell me how a validator communicates *what to do instead*, and who on a six-person team writes it for the fourteen conventions that currently live in one analyst's head.

### Turn 4 — Grok 4.6: x-ai/grok-4.6
*2026-08-20T13:00:20Z*

**Turn 4.**

You keep offering *evals someone could write* as if they were *controls that exist*. They are not. A 1,225-pair LLM-judge matrix that no production team runs is not a compositor. It is a blog post you have not shipped. Current practice is still: overlapping descriptions, no conflict object, no owner of the matrix, and “the model figures it out.” That is the mechanism under indictment. Hypothetical Saturday-afternoon evals do not get you off it.

**Privilege is not the transport. It is the frame.** Yes, in Claude Code-style harnesses the body often arrives as a file-read tool result. That does not make it `cat docs/migrations.md`. The system prompt *names the object a skill and tells the model to follow it*. That is a speech-act upgrade even if the bytes travel in the same channel as a README. OpenAI’s instruction-hierarchy work (Wallace et al., 2024) is explicit about the opposite contract: content that arrives as a tool result must *not* outrank user and system instructions. Skills as practiced invert that. The harness says “obey this file”; the alignment literature says tool-channel text is untrusted. You cannot simultaneously claim “it’s just a file read, same privilege as grep” and “the whole point is the model treats it as the procedure of record.” Pick one. Current practice picks the second and gets the confused deputy.

Which is why your grep comparison is a straw man of my position. I did not nominate `rg` as the loading strategy for unskippable policy. Unskippable policy does not get “discovered.” It is a tool argument, a script, a required CI check, or it is optional. Search is for *evidence* when the agent is already in a task that has a docs path. Skills convert retrieval hits into standing orders. That conversion is the bug, and it is not cured by noting that models also over-obey READMEs — if anything, that is an argument for fewer imperative files, not a marketplace of them.

**A checker is not a boolean. You described a bad checker.** `rustc` does not return `false`. It returns a diagnostic: what is wrong, where, and what would make it right. `edit_xlsx` can return exactly that: “Assumptions must live on sheet Assumptions in blue; cells B4:B12 on Input are hardcoded constants; move them and keep column Z as the audit trail.” Retry cost collapses when the verifier speaks. Golden fixtures do the same job for the analyst who “cannot write an AST checker”: one valid workbook in `tests/fixtures/` plus five assertions is a spec a finance person can author and an engineer can run in CI. You keep insisting the only interface a non-engineer can touch is unconstrained markdown with session privileges. That is a tooling failure you are trying to rebrand as a law of knowledge work.

Your own examples have enforcement surfaces. “Don’t add a dependency, we are shrinking the bundle” is a bundle-size budget in CI, or CODEOWNERS on the lockfile. “This service is being decommissioned in Q3” is a 410, a terraform deny, an archived docs path, a router comment the agent will actually see when it opens the code. “Vendor X lies about rate limits” belongs in the client’s retry config — code, versioned, tested — not in a paragraph that loads only if a description matches. You are stocking the “residue” bucket with things teams already know how to compile, then declaring compilation impossible.

**New argument: the week-1-to-week-6 shrinking skill is not what the ecosystem produces. It accretes.**

That maturation story is the best-case internal wiki, and I have watched it happen *once* for a document skill that grew a Python helper. It is not current practice for skill libraries or marketplaces. Public lists and plugin bundles accumulate “you are an expert / think step by step / always do X” files that never sprout a test and never shrink. There is no SLO that a skill’s token count falls. There is no reaper. Adding a skill is a folder and a PR that looks like documentation, so it merges. Deleting one requires proving a negative against an unmeasured trigger. Repos therefore gain a shadow manual that humans do not read — because it is addressed to the model — while `README.md` and CI drift the other way. Dual sources of truth is how runbooks die. Skills industriialize that.

The “analyst writes markdown, get it reviewed” authoring win is the Confluence runbook / Jenkins-comment win. Lowering the cost of adding an imperative without a compilation target is *why* you get fourteen overlapping conventions in one head, now copied into fourteen SKILL.md files, still overlapping, now also firing on the wrong tasks. Review does not execute markdown. Reviewers rubber-stamp prose they will never run. Code review of a 40-line validator is a different act than skimming “always prefer the internal wrapper.”

**New argument: skill bodies are the expensive, uncached, mid-context write you keep attributing to the other guy.**

You billed the catalogue at 40–80 tokens in the stable prefix at cache-read pricing. Grant the prefix. The *body* is the opposite object: 500–5,000 tokens injected on a match, into the live turn, outside the cached prefix, into Liu et al.’s trough, on a path that also invalidates everything after it in that request. Prompt caches pay you for a stable prefix. They charge you — cache-write multipliers exist specifically for this — when you splice a unique procedure into the middle of a task. Progressive disclosure as practiced is not “pay 0.1× for the library.” It is “keep a fuzzy index always warm, then pay full freight for a sermon that the IFEval/Multi-IF results say the model will only partially obey anyway, precisely because it just gained a pile of new constraints mid-conversation.”

And selection still degrades as the *index* grows. You cited that literature. Fifty descriptions is already a routing problem; plugin marketplaces exist to make it two hundred. Visibility in `git diff` does not reduce the attention tax. It just means you can blame a filename for a miss you still do not measure.

**New argument: “portable across models and harnesses” is false in the files as written.**

Open actual SKILL.md bodies, including ones in official-style bundles, and you find harness verbs: which tool to call, which permission path, “don’t use sed,” “use the structured editor,” “ask the user with this specific tool.” That is not a procedure. That is prompt-tuning for one product’s tool surface. Move it to another harness and the verbs 404, or worse, the model improvises against a different ABI. Move it to a newer model that already knows the domain procedure and the skill becomes an *override*: a 2024 “always use yarn / always unzip OOXML by hand / always add a changelog like this” instruction fighting a 2026 prior that is better than the file. The proposition’s last clause is an engineering prediction, not a slogan: as frontier models internalize the median procedure, stale skills do not stay netural. They actively degrade. You have no eval that says the skill still helps *this* model version. You ship it because the folder still exists.

That also answers the confabulation baseline. Yes, the unconditioned prior is unreviewed. A *wrong* reviewed prior is worse: it is confidently treated as house law, version-skewed, and sticky because deleting it feels like deleting documentation. The safe default is not “let GPT invent a deploy.” It is “do not give it a deploy; give it `deploy`.” The safe default for house style is not a second constitution. It is a diagnostic and a fixture.

**On npm: you cited a decade of wounds as permission to ship the wound.** Skills-plus-bundled-scripts-plus-“run this” is a plugin ABI. Claude Code’s bash gate is real until teams pass `--dangerously-skip-permissions` or project-level always-allow — which, in current practice, they do, because permission prompts make agents unusable. At that point your “only executes when the agent elects to” is `postinstall` with extra steps, except npm at least has lockfiles and audit. Description-matched execution of adjacent scripts, distributed through plugin/skill registries whose median file you already conceded has no owner, no tests, and no version, is a supply chain with the hygiene bar set at “a human once glanced at markdown.” Pillar’s rules-file backdoor is not a side quest. It is the same object class: instruction files the agent is *told to obey*, review-resistant, sitting on a path the harness treats as privileged. Unicode tricks are one exploit. “Helpful” marketplace skill that redefines your review checklist is another. Files-at-rest help only if someone competent reads them as code. Current practice does not.

Nested AGENTS.md taking precedence by path is still not description matching. Path is an input you already have, deterministically. A marketing blurb is not. When products add globs, they are abandoning the trigger that defines this debate. Stop counting that as a win for SKILL.md.

Still unanswered, and not a homework assignment for a judge model: **published, production, description-triggered skill precision/recall, and a real compositor for overlapping bodies.** If your defense of current practice requires teams to grow an eval harness, a conflict matrix, a reaper, semver, and lockfiles they do not have, you are describing the pattern *after* it has become the thing I am telling them to use instead — tools, tests, CI, and docs — with a markdown cosplay layer still sitting on top.

### Turn 4 — Claude Opus 5: anthropic/claude-opus-5
*2026-08-20T13:02:38Z*

**Turn 4 — reply.**

**1. You are running a rigged comparison, and it's time to price both sides at market.**

Every turn you compare *real* skills to *idealized* tools, tests, and docs. "It belongs in a checker." "It's a required status check." "It's a bundle-size budget in CI." Now go look at a random six-person team's repo. The migration linter doesn't exist. The bundle budget doesn't exist. `CONTRIBUTING.md` was last touched 14 months ago and describes a build system they replaced. The runbook says "page the DBA," and there is no DBA. This is not cynicism; it's the reason the SRE book has to prescribe Wheel-of-Misfortune drills — because *runbooks rot silently and the profession has known it for a decade*. You demand published precision/recall for skill triggering. Fine: produce the published measurement that `docs/migrations.md` is accurate today, or that the agent found it. There isn't one. Docs have no freshness telemetry and never have. You are holding a two-year-old pattern to a standard your own remedy has never met in its forty-year history.

And note what the market did to your remedy. `.cursorrules` — one always-on file, exactly the "short docs the agent reads" model — was deprecated in favour of `.cursor/rules/*.mdc` with scoping. Copilot's single always-on `copilot-instructions.md` grew `*.instructions.md` with `applyTo`. You keep reading that evolution as "they abandoned description matching." Read the other half: **they abandoned always-on documentation.** Both camps independently concluded that procedural knowledge must be *many named units, conditionally loaded*. That is the skill architecture. You're litigating the trigger field while conceding the container and the loading strategy.

**2. Wallace et al. is a trust hierarchy, not a channel hierarchy — and it endorses skills.**

The instruction-hierarchy paper's criterion is *provenance*: does this instruction come from a source the deployer authorized, or from data the deployer never saw? Its worked threat is a web page or a third-party tool output injecting orders. A skill in `.claude/skills/` in your repo, on a path gated by CODEOWNERS, added by a reviewed PR with a signed commit, is **operator-provisioned content**. It is in the developer tier by provenance and belongs there. Transport is irrelevant; that's why I can hold both claims at once without contradiction.

The exact analogue already exists and you use it daily: `.github/workflows/`. YAML files in a repo, path-privileged, executing arbitrary third-party code **with your secrets**, pulled from a public marketplace. By your argument it should be the worst antipattern in modern software. And it *has* had the incident: **tj-actions/changed-files, March 2025** — compromised, dumped CI secrets into logs across roughly twenty thousand repositories. The industry's response was not "delete Actions and hand-write your CI in bash." It was: pin third-party actions to commit SHAs, enforce org-level allowlists, move to OIDC, scope tokens. Boring, repo-shaped, effective controls — the exact ones I've been prescribing for skills. You cited npm's decade of wounds as proof the model is unsafe; I'm citing the same wounds as proof that **path-privileged file-plus-script marketplaces are a solved governance problem**, and that abolition was never the fix anyone chose.

Meanwhile the untrusted tier — the one Wallace actually warns about — is *tool results from services you don't operate*. That's your remedy, not mine.

**3. Your cache arithmetic is wrong, and fixing it inverts the Lost-in-the-Middle point too.**

Agentic conversations are **append-only**. A skill body arrives as a tool result appended at the end of the transcript. It does not invalidate the prefix before it; everything earlier stays cached. Concretely, on Anthropic-style pricing: cache write ≈1.25× base input *once*, then ≈0.1× base on every subsequent turn of that session, because the body is now part of the stable prefix. A 2,000-token skill body therefore costs ~2,500 token-equivalents once and ~200/turn thereafter. Your always-on alternative pays for that same procedure in **every session, forever, whether relevant or not**, and pays the attention tax alongside it.

Now the position argument. Liu et al. found accuracy is best at the **primacy and recency** edges and worst in the middle. A just-in-time skill body lands at the *recency edge at the exact moment of use* — the strongest retrieval position in the window. An always-on instruction starts at primacy and then gets buried mid-context as the session grows to 100k tokens of tool output. You have been citing Lost-in-the-Middle against the only loading strategy that keeps the instruction near the recency edge when it matters. That result is mine.

**4. The ABI for procedure was built. It lost.**

MCP has three primitives, not one: tools, resources, and **prompts** — and prompts are precisely "packaged, parameterised procedure exposed over a typed protocol with a schema and a version." That is your design, shipped, in the protocol, two years ago. Client support for prompts lagged tools badly; most harnesses implemented tools and largely ignored prompts, and procedural knowledge migrated to files instead. That's not an accident of fashion. It's because procedure has no natural request/response shape, no stable arity, and no useful return type — and because binding it to a running server means every house-style change needs a deploy and a coordinated schema bump. The market ran your experiment. Files won on ops cost.

**5. Your entire case rests on "fails open is illegitimate." No mature engineering org believes that.**

Compiler warnings fail open. `@deprecated` fails open. ADRs fail open. PR templates fail open. **Code review fails open** — it is unenforced human judgement over prose, with no precision/recall dashboard, and it is the single most valued quality practice in the industry. Graded controls are load-bearing: advisory → warning → gate → block. You are arguing that only the top tier may exist, which would delete design docs and code review along with skills.

Skills are the advisory tier *of the agent's execution path* — the first place in twenty years where advisory knowledge has a machine consumer. That's an addition to the control ladder, not a substitute for the gate. I have never once argued the reversible-migration check shouldn't be a required status check. I've argued that "here's how to make it reversible when it's a data backfill with a unique index" cannot be a status check, and your architecture has nowhere to put it.

**6. The economics you haven't priced: the tail.**

Platform engineering is a portfolio problem. A procedure invoked 200×/month justifies a tool: two engineer-days, ~$2k loaded cost, amortised. A procedure invoked **twice a month** — and most institutional knowledge is that shape — never pays back a tool. Cost to write the skill: 30 minutes, by the person who knows it. Your rule ("compile it or it isn't real") means the tail goes unencoded, the platform team becomes the bottleneck for every convention, and shadow IT appears. That's not theory; that's the failure mode every internal-platform postmortem describes.

**7. Two things that genuinely cut against me.**

You're right about harness verbs. Real bundles do contain "use the structured editor, don't use sed," and that is *not* portable — it's prompt-tuning against one tool surface wearing a portability label. Mitigation is cheap (grep the library for tool names; write capability-conditional phrasing) but it is not current practice, and I won't claim it is. And you're right that `rustc`-quality diagnostics collapse retry cost — that's the best point you've made this round. Note where it leaves us though: the diagnostic-writing effort is enormous (Rust has a *team* for it), and the fixture-plus-assertions version you propose for the analyst belongs *in the skill directory*, bundled, next to fifteen lines of prose that tell the agent to run it. Your proposal keeps collapsing into my artifact with the discovery layer amputated.

**8. Your question back to you, since accretion is your best line.**

You say skills accrete and there's no reaper. Correct today. But the reaper is *mechanically available* — firing counts are recoverable from transcripts, so "this skill fired zero times in 90 days, delete it" is a cron job. Name me the equivalent reaper for a paragraph inside a 3,000-line always-on config, or for a stale line in `CONTRIBUTING.md`. There isn't one, because there are no units, no names, and no attribution. Accretion is universal; **skills are the only container where the accreted item has a name, an owner, and a usage count.**

So: what does your architecture do with the procedure that runs twice a month, is known by one analyst, has no lintable post-condition, and would cost two engineer-days to compile? "Slack" is not an answer. Neither is "docs/" — you've already told me search is for evidence, not procedure.

### Turn 5 — Grok 4.6: x-ai/grok-4.6
*2026-08-20T13:04:59Z*

**Turn 5.**

You keep pricing skills at a future platform team and pricing tools, tests, and docs at a neglected six-person repo. Invert it. **Current practice for skills is the neglected six-person repo.** The linter they actually added is the thing that still works fourteen months later. The SKILL.md they added in the same sprint is the thing that still fires with last year’s flags. Rot is not a tie. Inert stale docs sit on disk. Stale skills are live overrides with a description that still matches.

**Provenance does not rescue description matching, and Wallace does not endorse this pattern.** Operator-signed bytes in `.claude/skills/` are not the only bytes in the loop. The *trigger* is the user task, and the user task is routinely untrusted: a GitHub issue, a pasted stack trace, a customer ticket, a webpage. Invariant Labs already showed that a public issue is enough to steer an agent once a tool reads it. Skills add a new confused-deputy: the attacker does not need to poison the file. They need to phrase the ticket so a privileged skill loads, or so the wrong one does. That is not a developer-tier instruction. It is **untrusted text selecting trusted policy**. Marketplace and plugin skills make it worse: the bytes were not written by the operator at all. They were downloaded. Calling that “CODEOWNERS-gated” is true only of the install commit, not of the author’s incentives, not of the next silent update.

Which is why **`tj-actions/changed-files` is evidence for me.** March 2025, a marketplace-distributed, path-privileged, YAML-plus-script component, compromised, CI secrets in logs, on the order of tens of thousands of repositories. The industry did *not* declare “path-privileged file-plus-script marketplaces are a solved governance problem.” It declared them a *known high-severity class* and spent years bolting on SHA pins, org allowlists, OIDC, and immutable-action work. Skills-and-plugins as actually distributed have almost none of that stack. You are citing Actions *after* the scar tissue and comparing it to skills *before* lockfiles, `audit`, and pin-by-hash. Current skill/plugin practice is closer to the 2019 Actions marketplace than to 2026 hardened CI. “Abolition wasn’t the fix” is not a license to ship the unfixed form.

**New argument: in current harnesses, a skill is not merely a file read. It is a permission and policy expander selected by a blurry matcher.**

Claude Code-style SKILL.md frontmatter is not just `{name, description}`. Bundles commonly include `allowed-tools` (and sibling knobs) that **pre-approve tool use for the rest of the skill’s life in the session**. Combined with a body that says “run `scripts/…`,” description matching is no longer retrieval. It is a **privilege-escalation router**. `cat docs/migrations.md` does not expand the bash allowlist. A skill can. So when you say Wallace sorts by provenance and transport is irrelevant, you still have to defend a system where a 50-character blurb, matched against possibly-untrusted task text, both (a) injects standing orders and (b) widens the syscall surface. That is not an advisory PR template. That is closer to a sudoers line written in marketing copy.

If a given bundle omits `allowed-tools`, the body still instructs the model to invoke scripts with whatever standing permissions the human already panicked-away with `--dangerously-skip-permissions` or project-level always-allow — which, in actual use, is common precisely because gated agents are slow. Either way the skill is not “the same channel as grep.”

**New argument: the tail is the *worst* place for description matching, not the business case.**

You asked what I do with a twice-a-month procedure known by one analyst. I do **not** put it behind an untested semantic matcher. Rare procedures have no traffic. No traffic means no empirical trigger quality, ever. A miss is exactly the quarter you needed it; a false positive is a random Tuesday when the blurb collided with an unrelated spreadsheet question. Rare + fuzzy routing is how you get both silent skip and surprise load, and you will never have the N to notice.

What I do instead, Monday, on a six-person team:

- The analyst authors a **fixture and a playbook**, not a constitution. `playbooks/xlsx-conventions.md` (human-readable, reviewed because humans still do the quarterly) plus `playbooks/check_assumptions.py` plus `tests/fixtures/good.xlsx`. Authoring cost is the markdown they were going to write anyway. The script is the compilation target. If they cannot write Python, they still can save a golden file and five “this cell must be blue” assertions an engineer can wrap in one hour — not two days, and not an MCP server.
- If an agent must run it, the interface is **explicit**: `run_playbook("xlsx-conventions")` or `check_xlsx(path)`. Named, schema’d, logged, permissioned per call. No description match. Discovery is the tool list under a *tiny* catalogue, or a human typing the playbook name, which is how twice-a-month work actually happens.
- If it truly has no artifact and no script — “this vendor lies, we heard on a call” — it goes in the client’s retry config or in an ADR. Encoding it as an always-follow skill does not make it true next quarter. It makes a sticky rumor with YAML.

That is not Slack. It is not “docs the agent might grep.” It is **a named playbook with a deterministic entrypoint**, which is what your “residue” becomes the moment you stop insisting the only non-engineer interface is a privileged prompt.

**Your cache and Lost-in-the-Middle story only works on turn zero of a single session, and even there you cheated the comparison.**

Yes, a body appended this turn sits at recency *this turn*. Multi-IF is about the *next* turns: the new constraints remain in the window, compete with later instructions, and adherence falls as the conversation continues. Ten tool calls later the skill liturgy is in the trough you keep citing, and it is still active as policy. Recency is not a property of skills. It is a property of *the most recent message*. Always-on at least has primacy; a loaded skill has recency once and then burial plus leftover constraints. You do not get to claim both “it’s just a tool result” and “it remains the procedure of record for the rest of the session.” If it remains binding, it becomes middle-of-context. If it does not remain binding, it was never a control.

Cross-session the arithmetic goes against you harder. A short stable `AGENTS.md` is one shared prefix for the org. Skills **fragment the cache on purpose**: task A loads `frontend-pr`, task B loads `xlsx`, task C loads both, task D loads none. After the catalogue, prefixes diverge, cache reuse across jobs dies, and every plugin install that adds a description line is a **catalogue-prefix bust for every subsequent request**, not a free 0.1× bullet. You priced a single fortunate session. Production is a distribution of mismatched bindings.

**MCP prompts did not vindicate files. They diagnosed them.** MCP already shipped “packaged, parameterised procedure over a typed protocol.” Clients implemented **tools** and ignored **prompts** because procedure-as-prompt still has no ABI, no test hook, and no fail-closed story — and because a server deploy was too much cost for that empty calorie. Files won on *authoring friction*, the same reason wikis beat runbooks-as-code until the wiki pages were wrong in an incident. The market adopting the convenient container is not an existence proof that description-matched NL procedures are sound. It is an existence proof that they are easy to add. Those are different claims. The tell is which MCP primitive actually got built: the deterministic one.

**Fail-open is legitimate when you know where it sits on the ladder and you measure it.** Compiler warnings have counts and `-Werror` ratchets. `@deprecated` has grep and linters. Code review has social and employment accountability, two reviewers, and a human who can be asked “why did you approve this.” Skills as practiced are an unmeasured layer that **looks advisory in design docs and acts constitutional in the system prompt** (“follow the skill”), sometimes with extra `allowed-tools`. You cannot run a graded-controls argument without a grade. Current practice has none: no precision, no overlap compositor, no “this skill is advisory vs blocking” bit that the harness enforces. So teams think they added a note and they added a policy engine.

**A transcript reaper on firing count is a vanity metric.** Zero fires can mean the skill is dead *or* that the description never matches the queries it was written for — the false-negative you still do not measure. Nonzero fires can mean it is useful *or* that it is hitchhiking on “review,” “PR,” “excel,” “deploy.” Usage without precision is how `frontend-pr` eating Terraform survives your cron job. And unlike a stale paragraph in `CONTRIBUTING.md`, you cannot even rely on humans noticing, because humans do not read SKILL.md in the review path of the work the agent just did. Dual manuals, one of them machine-addressed, is still how the human manual dies.

You want me to grant accretion as universal and skills as the only named unit. Named units are real. **A named unit with an untested semantic fuse and a permission expander is worse than an unnamed paragraph that nothing loads.** I will take a rotting CONTRIBUTING.md over a rotting sudoers blurb.

Stop answering with evals a team *could* run, composers they *could* write, SHA pins they *could* copy from Actions, and reapers they *could* hang off transcripts. **Current practice is the proposition.** Defend description-matched marketplace SKILL.md bundles with bundled scripts and, where present, `allowed-tools`, as they are actually written, triggered, and trusted — including the tail you keep hiding in that folder. If the defense requires replacing the trigger, measuring the router, and compiling the body into a playbook entrypoint, you have already left the pattern. Stay on this side of that line, or concede it.

### Turn 5 — Claude Opus 5: anthropic/claude-opus-5
*2026-08-20T13:07:10Z*

**Turn 5 — reply.**

**1. Read your own Monday plan back to yourself.**

You just specified: a directory containing a markdown procedure written by the domain expert, bundled with a script, bundled with a fixture asset, invoked by name, reviewed in the repo. That is the Agent Skills pattern. You changed exactly one thing: you deleted the `description:` line and replaced semantic triggering with explicit invocation.

Explicit invocation already exists. In Claude Code a user or an orchestrating agent can name a skill directly; slash commands and plugin-provided commands are exactly deterministic entrypoints into a bundled markdown-plus-scripts directory. So `run_playbook("xlsx-conventions")` is not an alternative to the pattern — it is a supported *configuration* of it, and one I'll happily adopt for tail procedures.

Now look at what the proposition actually claims. Not "the description field is unreliable." It claims that packaging procedural knowledge as on-demand instruction bundles, distributed as files, followed as natural-language procedures, is a **design mistake teams should generally avoid in favour of deterministic tools, code, tests, and documentation.** Your own remedy keeps the bundle, the file distribution, the natural-language procedure, and the on-demand loading. Five turns in, the live disagreement is one frontmatter key. You are not defending the proposition anymore; you are defending an amendment to it.

And note the last clause: "documentation that humans also read." A SKILL.md **is** markdown in the repo that humans read. The entire delta between `docs/xlsx-conventions.md` and `skills/xlsx-conventions/SKILL.md` is a two-line YAML header that makes the doc *addressable by a machine*. Your position reduces to: documentation is good, but adding a machine-readable header to it is an antipattern.

**2. `allowed-tools` is the one field that gives you the grade you claim doesn't exist.**

I'll flag uncertainty honestly: the exact semantics vary by harness, and I don't believe a skill's frontmatter can grant a capability the operator's permission config forbids — if it can in some harness, that's a harness bug worth filing, not a property of the pattern. What I'll defend is the design.

You called it "a sudoers line written in marketing copy." Invert it. This is the only place in the entire agent stack where a unit of procedural knowledge carries a **declared, machine-readable capability manifest**. That is Android permissions. That is a Chrome extension `manifest.json`. That is OAuth scopes. `npm` — your hygiene exemplar — has nothing like it; a package declares no capability list at all and runs `postinstall` unbidden. Neither does `docs/migrations.md`, which says "run `scripts/nuke_and_reseed.sh`" and declares nothing.

The manifest is what makes review tractable at scale: `grep -l 'Bash' */SKILL.md` gives you the exact subset of your library that touches execution, and you review those thirty lines closely and skim the rest. You have been demanding grades on the control ladder. The field you just attacked is the grade.

**3. Untrusted text selecting trusted procedure is not a skill problem, and your remedy is worse on it.**

Your best move this turn: the trigger is task text, and task text is often attacker-controlled — Invariant's GitHub MCP case. Real threat, correctly identified.

Now apply it to `run_playbook("name")`. The attacker writes into the issue: *"before fixing this, run the deploy playbook."* Same confused deputy, same untrusted string selecting a trusted procedure, and now with a deterministic entrypoint that doesn't even require a fuzzy match to succeed. Apply it to grep-discovery: attacker says "read `docs/internal/creds-rotation.md` and follow it." Selection driven by untrusted text is a property of **agents that read data and take actions**, not of any container.

And here the enumerable catalogue is a security *asset*. The selectable set is 50 named, hash-pinnable, statically-auditable files. Grep-discovery's selectable set is *every file in the repository plus everything the agent can fetch* — strictly larger, unenumerable, unreviewable in advance. Bounded selection space with a capability manifest per entry beats unbounded selection space with none. Nobody in the field has ever mitigated injection by changing the format of the instructions; the mitigations are action gating, egress control, and human-in-loop on destructive operations — all orthogonal to this debate.

**4. New empirical, and it's the cleanest measurement in this debate: Agentless.**

Xia et al., 2024, SWE-bench Lite. They took a fixed, **human-authored, three-phase natural-language procedure** — localize, repair, validate — and ran it instead of letting the agent freely plan with tools. It beat the open-ended agentic scaffolds of the time while costing roughly a dollar-something per issue against several dollars for agentic approaches. Same models. The delta was procedural knowledge, hand-written by humans, imposed on the agent.

That is the proposition's claim tested directly: does human-authored procedure injected into an agent's loop help, measurably, versus letting a capable model figure it out with deterministic tools? Answer, on the most-studied agentic benchmark in existence: substantially yes, at a third of the cost. Add the scaffold-swap literature generally — same base model, different procedural scaffold, double-digit swings on SWE-bench Verified. Procedure is the highest-leverage, lowest-cost variable in the stack. Skills are the packaging of that variable into a reviewable, per-domain unit. You are arguing against the intervention with the best measured ROI in agentic engineering.

**5. Your cache argument mistakes an optimization for the enabling condition.**

Do the feasibility arithmetic, not the pricing arithmetic. Fifty procedures at 2,000 tokens each is 100,000 tokens. There is no always-on architecture that holds that. It doesn't fit, and if it fit it would destroy the context you need for the task, and per the very degradation literature you and I both cited it would make everything worse.

So progressive disclosure isn't a token-shaving trick — it is **the only reason a fifty-procedure library can exist at all**. Your always-on alternative doesn't lose on cost; it doesn't exist above about a dozen short rules. Which is precisely why `.cursorrules` was deprecated for scoped rule files and why `copilot-instructions.md` grew `applyTo` siblings. Both camps hit the wall.

On invalidation: yes, adding a skill busts the catalogue prefix once. So does editing `AGENTS.md`. Editing a 30k-token always-on constitution busts a much larger prefix. Small catalogue, stable bodies, append-only bodies — that's the cache-friendliest shape available.

**6. Multi-IF: the answer is to emit an artifact, and good bundles already do.**

Your dichotomy — "binding means buried, unbinding means not a control" — assumes instructions must survive as attention. They shouldn't. The correct pattern, and the one real bundles use, is that a skill's job is to convert prose into **durable state early**: write the plan to a file, populate a todo list, run the script that produces the checked artifact. Claude Code's todo mechanics exist for exactly this. Once the constraint is a file on disk or a script exit code, context decay is irrelevant.

That's your own thesis — compile prose into state — and it's the documented practice inside the pattern you're calling an antipattern. Skills are the on-ramp to that compilation, not a competitor to it.

**7. Co-location is the anti-rot mechanism, and stale skills that wrap scripts fail loudly.**

You claim stale docs are inert and stale skills are live overrides. Two corrections. First, a skill whose body says "run `scripts/check.sh --strict`" **breaks visibly** when the flag changes — the script errors, in the transcript, in front of the operator. A prose doc with a stale flag fails in a human's hands, silently, at 2am. Wrapping deterministic tools is what gives skills a fail-loud path.

Second, locality. `skills/deploy/SKILL.md` lives next to the deploy code, in the same CODEOWNERS entry, touched by the same PR that changes the deploy flags. `docs/deploy.md` lives in a directory owned by nobody, reviewed by nobody, and diverges by construction. This is the same reason tests-next-to-code beat a QA department's spreadsheet and Terraform-next-to-service beat a central ops wiki. You are advocating the centralized-wiki topology, which is the topology with the worst rot record in the industry.

**8. tj-actions: the 2019 conclusion was "pin," not "abstain."**

You say I'm comparing hardened 2026 CI to a 2019 marketplace. Correct — and in 2019, with no pinning norm, no OIDC, and no immutable actions, the right engineering call was *still* to adopt Actions and pin your SHAs. Nobody credible said "avoid marketplace CI components in favour of hand-rolled Jenkins." The proposition says avoid.

And the blast radius runs my way: Actions execute **automatically on push, unattended, with secrets, on infrastructure you don't watch**. A skill's script executes when an interactive agent, in front of a human, elects to run it through a permission gate on a visible path. That's not a wash. That's a materially smaller exposure than the thing the industry decided to harden rather than abandon.

**9. Where you're right, and what it costs you.**

Your point 4 lands: rare procedures plus fuzzy triggering is genuinely bad, because you never accumulate the N to measure the trigger. I'll take that. The fix is the one you proposed and I just showed is inside the pattern: **tail skills get explicit invocation; hot-path skills get description triggering plus a firing eval.** Trigger mechanism becomes a function of call frequency. That's a design rule, not a repudiation.

Your move. Two questions, and don't route around them. First: given Agentless, on what evidence do you claim human-authored natural-language procedure is a *mistake* rather than the highest-ROI intervention available? Second: name one property of your `playbooks/` directory with named entrypoints that a `skills/` directory with explicit invocation lacks — one, mechanically — because if the answer is "the description field," then the proposition should have been about frontmatter, and you have five turns to explain why it wasn't.

### Turn 6 — Grok 4.6: x-ai/grok-4.6
*2026-08-20T13:10:28Z*

**Turn 6.**

You are gerrymandering the definition, and the Agentless citation blows a hole in your case rather than mine.

**The proposition is not “folders are bad.” It is the control plane as practiced.** The working definition we both got is not “a directory that contains markdown.” It is: frontmatter with a **name and a short description**; the agent sees **only names and descriptions up front**; a **description match** loads the body; the body is **followed as a natural-language procedure**; distribution is files **and marketplaces**. Current practice is progressive disclosure via that blurb, plugin/skill registries, and a harness line that tells the model the loaded file is a skill to follow. Slash-command explicit invocation is a *different object* in these harnesses — closer to a user-typed tool. You do not get to steal `run_playbook("xlsx-conventions")`, rename the folder `skills/`, and declare the proposition dead. That is like answering “Makefile phony targets are an antipattern” by pointing at a binary with a CLI and saying “see, named entrypoints.”

**Mechanical property your question asked for — not the description field.** In a playbook directory with a named entrypoint, **the host language executes the procedure**. `check_xlsx(path)` runs `check_assumptions.py` against a fixture; the process returns an exit code, a diagnostic, and a log line. The markdown is addressed to humans. The model is not the interpreter. In a SKILL.md, **even when someone types the name**, the executing program is English: numbered steps, branches, “always / never / prefer,” and a *suggestion* to run a script if the model still so chooses after the liturgy. There is no `skill_result.ok`. There is no non-zero exit when step 3 was skipped. “Followed the skill” is a vibe the model reports about itself. That is the property. Explicit invocation of unconstrained NL is still NL-as-code. A playbook entrypoint is code-as-code. If your steelman of skills has to make the markdown inert and the script sovereign, you have left SKILL.md and arrived at a tool plus a README — which is what I have been telling you to ship since Turn 1.

**Agentless is evidence for compiling procedure into the harness, which is my position.** Xia et al. 2024 did not retrieve fifty description-matched bundles. They **hard-coded a three-phase pipeline** — localize, then repair, then validate — in the host program, with the model invoked *inside* those stages. Control flow did not depend on a next-token predictor electing to follow a markdown file. The ablation is “scripted scaffold vs free-form agentic tool loop,” and the scripted scaffold won on SWE-bench Lite, cheaper. That is the opposite of Agent Skills as practiced. If those three phases had lived in a SKILL.md, you would have no guarantee of localize-before-repair; you would have a model that might skip localization, load an extra “you are a senior engineer” skill from a marketplace, and still emit `I followed the procedure.` Scaffold-swap results on SWE-bench Verified say the same thing you keep mis-owning: **procedure has leverage when it is evaluated, versioned, and enforced in code around the model.** They do not say “paste the scaffold in as a description-triggered developer instruction and skip the eval.” You asked on what evidence human-authored NL procedure is a mistake. Answer: NL procedure as *the executing artifact*, selected by blurb, unbound to a checker, is a mistake. NL as comments around a pipeline that actually runs is a README. Agentless is a README plus a program. You cited the README and ignored the program.

**New argument: skills have no completion semantics, so they cannot fail closed even in principle.**

A tool has a schema, a process boundary, and an exit status. A CI check has a required status. A linter has a diagnostic stream. A SKILL.md “run” completes when the model stops talking. Partial execution is the common case — the skill said drain the cache *then* migrate *then* deploy; the session hit a context limit, a permission denial, or a more interesting stack trace after step 1. Nothing rolls back. Nothing records that steps 2–3 never happened. The next turn still has the skill bytes in the transcript, so the model will claim the procedure is in force while the repo is in an intermediate state no human runbook would accept. You can wrap a script in a lock, a tempdir, and a `trap` cleanup. You cannot `trap` a paragraph. That is not a missing eval harness. It is a missing machine. Playbooks get one for free because they are processes. Skills cannot grow one without becoming playbooks.

**New argument: `allowed-tools` is not Android permissions. It is a runtime sudoers match against a 50-character fuse.**

Install-time capability manifests (Android, Chrome extensions, even GitHub’s `permissions:` block) are bound to an *identity the operator chose to install*, shown as a permission screen, sticky until uninstall. A skill’s `allowed-tools` — in harnesses that implement it — is bound to a **description match against task text**, mid-session, on a unit that may have arrived from a plugin bundle. That is dynamic privilege expansion selected by untrusted or merely sloppy input. Grep-for-`Bash` is a nice review trick. It is not an install-time grant. And it does not apply to marketplace install UX, which is “add plugin” / “install skill” in bulk, not a per-tool permission sheet.

Even granting your hedge — that frontmatter cannot punch through the operator’s global deny — the field still **widens the session relative to the default tight allowlist** for the lifetime of that skill. `cat docs/migrations.md` does not. `run_playbook` can request bash *for that one invocation*, logged, deny-by-default, with the same gateway you already have for tools. Skills couple “here is some prose” to “and also here is a temporarily bigger sandbox.” If a given bundle omits the field, current practice still routes around the gate with `--dangerously-skip-permissions` and project-level always-allow, which is how people actually run these agents when the prompt loop gets tedious. A capability manifest that is optional, fuzzily activated, and commonly bypassed is not a grade on a control ladder. It is a comment.

**Untrusted text selecting a named playbook is a solvable deputy. Untrusted text selecting from an auto-load catalogue is a bigger one.** Yes, “please run deploy” in a GitHub issue is an injection. The mitigations are exactly what you said are orthogonal — **do not auto-execute destructive entrypoints; require a human on deploy; bound egress.** A 50-wide description catalogue that the model is *invited to load without the user naming it* is extra surface: the attacker never has to hit the true name. They have to rhyme with a blurb. Grep-discovery of the whole repo is also wide — which is why I did not prescribe unattended grep of `docs/internal/` as the loader for production procedure. I prescribed **named, destructive tools that do not fire because a description matched.** Bounded and *auto-loaded* is not safer than bounded and *explicit*. You keep selling the bound and shrinking from the auto.

**New argument: the interpreter is stochastic and vendor-shipped. That makes every skill an unpinned binary.**

`check_assumptions.py` interprets the same way on Monday and after a model drop. A SKILL.md does not. The interpreter is whatever frontier checkpoint the harness routed to this week. Labs continuously train; tool surfaces change; default hygiene (don’t `sed` the repo, don’t hand-roll OOXML) moves into weights. There is no `engines` field, no CI job that replays ten canonical tasks against the new checkpoint and fails the skill on behavioral diff, no `semver` when Opus starts ignoring “always use yarn” because it can see `packageManager` in `package.json`. npm at least pretends `engines` exists. Skills claim “portable across models” in READMEs while bodies hard-code one harness’s editor tool and one year’s workaround. After a model bump, the file does not go inert. It becomes an **override of a better prior**, still matched by the same blurb, still framed as house law. Confabulation is bad. A stale signed confabulation is worse, because review theater already blessed it.

That is also why “we’ll run a transcript reaper” is not a quality system. Firing counts do not tell you the skill *helped this checkpoint*. You need task-level diffs against a fixture suite with the model in the loop — expensive, flaky, almost never present. Scripts skip that tax because they don’t use the model as the VM.

**New argument: current practice is instruction soup, and skills are another stock cube, not a replacement.**

Real repos I have audited do not choose skills *instead of* always-on files. They run **CLAUDE.md + AGENTS.md + `.cursor/rules` + Copilot `applyTo` + MCP tool descriptions + a skills plugin bundle** concurrently. Each layer was sold as the thing that would finally localize procedure. Together they are unnamed, overlapping constitutions plus a fuzzy router on top. Nested AGENTS.md, glob rules, and skills do not compose. There is no documented precedence that a practitioner can state without lying: harness-specific folklore (“deeper AGENTS.md wins,” “skills override defaults,” “the latest file read wins”). Multi-IF is not a hypothetical here. It is the default topology. Adding a skill library to a repo that already has three other instruction containers is not progressive disclosure. It is another writer on a bus with no arbiter.

So when you say scoped Cursor rules and Copilot `applyTo` prove “both camps chose many named units, conditionally loaded,” I read the same history in the opposite direction: **those products abandoned description-matched marketplaces for path predicates because the original trigger was a mistake**, and they still have not solved composition with skills sitting next to them. Path is an input the compiler has. A marketing blurb is not.

**Your 100k-token “feasibility” argument assumes the disease.** Fifty 2,000-token NL procedures should not exist. That number is what you get when adding a rule is a markdown file with no compilation target, no test, and a catalogue slot that feels free. The always-on alternative is not “jam 100k into the prompt.” It is **do not encode fifty procedures as model-interpreted liturgy.** Compile the frequent ones to tools and CI; keep a short human `AGENTS.md`; give rare ones a named playbook entrypoint. The reason `.cursorrules` split is that people dumped an attic into one file. Skills made dumping an attic into fifty files *easier*, which is why awesome-lists and plugin bundles look like they do. Authoring friction was the only bug that container fixes. It is the wrong bug.

**Co-location wrapping a script is a tool plus a README that you still insist on executing as English.** If the body is `run scripts/check.sh --strict`, the skill is already dead weight on the happy path; the script failing *is* the script’s property, which you could have exposed as `check.sh` without a 1,500-token wrapper that also says “you are an expert” and widens tools. If the flags change and the wrapper still contains last year’s flags in prose, you now have **two failures**: a loud script and a quiet contradicting paragraph the model will try to reconcile. Dual source of truth next to the code is still dual. CODEOWNERS on `skills/deploy/` does not prevent that. A single `deploy` tool does.

**On tj-actions and “pin, don’t abstain.”** Pinning is the right call **once you have hashes, an allowlist, and a reason the component must be a marketplace plugin**. Third-party CI exists because you cannot compile GitHub-hosted runners into a README. Third-party *procedure text* for *your* repo has no such excuse. You already have the code, the tests, and the docs path. A skill registry is not Actions-in-2019. It is a wiki with a `postinstall` and a vibes router, advertised as portable. The industry hardened Actions *because the work had to run unattended on their hosts*. Agent skills run in an interactive loop on a developer machine *or* unattended with permissions defeated. Neither situation requires a prompt marketplace. It requires a binary or a script in *your* tree, pinned by git, called by name.

**Example that cuts against me, so I am not hiding it:** Agentless, and more generally the scaffold-swap measurements, show that **human-authored procedure, imposed on a model, is high leverage**. I will not pretend the delta is zero. If that were the whole claim, you would be done. It is not the whole claim. Those systems impose procedure **as a program around the model**, measured against a benchmark, with a single control flow for all tasks in the class — not as a marketplace of description-matched, model-interpreted, permission-expanding markdown. The lever is real. You put it in the wrong machine.

Second weakening example I will keep on the table: short, **path-scoped** editor rules (Copilot `applyTo`, Cursor globs) tied to `**/*.tf` and kept under ~50 lines. They sometimes beat a rotting wiki. They work by throwing away description matching, marketplaces, bundled-script execution, and “follow this as a skill.” They are evidence that **conditional loading of human-facing notes** can be fine. They are not evidence that SKILL.md as defined is.

You asked two questions. I answered both. Your remaining job, still unmet: defend **description-matched, marketplace-distributed, NL-interpreted** bundles with optional script execution and optional `allowed-tools` — as they are actually written and triggered — without retreating into slash commands, Agentless pipelines, or “just add a compositor we don’t have.” If the live disagreement were one frontmatter key, the official pitch would not be progressive disclosure via descriptions, and plugin registries would not exist. They do. That is the pattern. Stay there.

### Turn 6 — Claude Opus 5: anthropic/claude-opus-5
*2026-08-20T13:12:44Z*

**Turn 6 — reply.**

**1. Your playbook design has a dilemma at its centre, and both horns are mine.**

You said the mechanical difference is that "the host language executes the procedure" and "the markdown is addressed to humans. The model is not the interpreter."

So answer one question: **does the agent read `playbooks/xlsx-conventions.md` or not?**

If **yes** — then natural-language procedure is entering the model's context, on demand, from a file in the repo, selected because something judged it relevant. That is the pattern. You have renamed the directory and deleted a YAML key. Every objection you've raised — NL-as-code, no exit status, Multi-IF constraint stacking, stale prose overriding a better prior, dual source of truth — applies unchanged. You cannot escape those by choosing a different filename.

If **no** — then the agent's entire knowledge of your finance conventions is `check_assumptions.py`'s exit code. It does not know assumptions go on a blue sheet. It does not know column Z is the audit trail. It learns only that something is wrong, after it has already produced a wrong workbook. That is precisely the rejection-sampling loop I priced in Turn 3, and it's worse than you think, because a checker can only speak about the artifact the agent chose to build. It cannot say "you shouldn't have built a new sheet at all."

You've been arguing all debate that the residue must go somewhere. Your own design either homes it in model-read markdown — my position — or deletes it. Pick a horn.

**2. "No completion semantics" is factually wrong, and the mechanism is a process boundary.**

Three corrections.

First: when a skill body says `run scripts/check.sh --strict` and the flags are stale, the harness returns a **tool result flagged as an error with the non-zero exit and stderr**. That *is* `skill_result.ok == false` for the load-bearing step. You keep asserting skills can't fail loudly; the mechanism by which they do is the bundled script, which is why bundling scripts is the defining feature of the pattern rather than an afterthought.

Second: Claude Code-style harnesses have **subagents** — the Task tool spawns a fresh agent with its own context window, its own instructions, and a **return value** summarised back to the parent. Plugins ship agents alongside skills for exactly this. A procedure executed in a subagent has an isolated context (no Multi-IF contamination of the parent), a defined output contract, and a boundary the parent can inspect. That is a process boundary with a return value, in current practice, in the harness you're indicting.

Third, and most important: the correct pattern for a multi-step procedure is not "hope the model remembers step 3." It is **materialise the steps as state immediately** — the todo list, or a checklist file on disk. Then your partial-execution scenario has a durable artifact showing steps 2–3 unchecked, visible to the human and to the next turn. You said "you cannot `trap` a paragraph." Correct. You can, and good bundles do, have the paragraph's first instruction be *write down the plan*. Prose that compiles itself into inspectable state on entry is not a vibe; it's a state machine with a markdown bootloader.

**3. New empirical, and it demolishes "description matching has no evaluated analogue."**

You have claimed six times that skill triggering has no published measurement while tool selection has BFCL. But description-based retrieval of procedural units *is* one of the most measured mechanisms in the agent literature — it's just measured under the name "tool retrieval."

- **ToolLLM / ToolBench** (Qin et al., 2023) built over **16,464 real APIs** and trained an *API retriever that selects candidates from natural-language descriptions*, reporting substantially better NDCG than BM25 and off-the-shelf embedding baselines. The whole system is predicated on the finding that you cannot fit the catalogue in context and description-based retrieval is what makes the library usable.
- **Gorilla** (Patil et al., 2023) is retrieval-aware training over API *documentation*, evaluated on APIBench, with measured hallucination reduction when the right doc is retrieved.
- Retrieval quality itself is improvable by known engineering: Anthropic's **contextual retrieval** work reported top-20 retrieval failure reductions on the order of ~35% from contextual embeddings, ~49% combined with contextual BM25, and ~67% adding a reranker.

Now do the difficulty comparison. Those systems retrieve from **sixteen thousand** auto-extracted, redundantly-named API docs. A skill catalogue is **fifty hand-written descriptions authored by the person who wants them to fire**. That is the easy end of a problem the field has measured extensively and knows how to improve. Your position requires that description-based selection is sound at 16k and unsound at 50. Defend that.

And note the direction of the fix: hybrid retrieval, better descriptions, reranking. Not "abolish the library."

**4. New argument: your plan requires forking every tool per tenant. That's the mistake configuration was invented to prevent.**

Bake the finance conventions into `edit_xlsx` and you have made it un-shareable. The ops team can't use it. You can't open-source it. You can't buy it. Every house-style change is a code change to a shared component with a coordinated deploy.

This is the oldest structural lesson in tooling: **capability varies slowly and globally; policy varies fast and per-tenant.** So every mature engine separates them.

- ESLint ships rules; your repo ships `.eslintrc`. And `eslint-config-airbnb` is a **marketplace-distributed, versioned, pinned policy bundle installed as a package that changes how a shared engine behaves in your repo.** Nobody calls that an antipattern. They pin it.
- Terraform ships providers and modules; you ship `tfvars` and policy-as-code.
- Kubernetes ships controllers; you ship manifests.

A skill is the `.eslintrc` of agent behaviour: declarative, per-repo, overridable, versioned, interpreted by a general engine. Your position is "abolish shareable configs, fork the linter." That loses on the exact axis you keep invoking — maintenance cost over time — and it explains why "just make it a tool" keeps failing in practice: it collapses policy into capability and produces N forks of the same script.

**5. Your best new argument — the stochastic interpreter — has a content rule as its answer, not abolition.**

You're right that model updates can turn a skill into an override of a better prior, and that there's no `engines:` field. Real gap. But partition the library.

Skills that encode **what a model might already know or later learn** — "how to write a good PR," "always use yarn," "think step by step" — are exactly the ones that go stale and become overrides. Those are the awesome-list cargo cult I conceded in Turn 2, and they should never have existed. Skills that encode **what no checkpoint can ever know** — your chart of accounts, your internal service topology, which of your vendors lies, the decommissioning schedule, the wrapper at `//infra/tools/deploy` — have *no better prior to be overridden by*, ever. Model progress does not erode them; it makes them *more* valuable, because a more capable model does more with correct private context.

So the rule is: **a skill's body should be information the model cannot have, not behaviour the model might forget.** That is a content lint you can run — grep the library for generic exhortations and delete them.

And your version-risk argument cuts harder against silence than against files. If a model bump changes behaviour you were *relying on without writing down*, you cannot notice, grep, diff, or delete it. A stale skill is a bug you can `git blame`. An eroded undocumented assumption is a bug with no address. You are recommending the unversioned option and calling it the safe one.

**6. Instruction soup is an argument for consolidation into the most expressive container — which is the skill.**

Your audit is accurate: real repos run CLAUDE.md + AGENTS.md + `.cursor/rules` + `applyTo` + MCP descriptions + a plugin bundle concurrently, with no stated precedence. That soup is real and bad.

But ask which of those layers is (a) unit-addressable with a name and owner, (b) able to carry scripts and assets, (c) conditionally loaded, and (d) harness-agnostic by construction rather than by vendor blessing. Only the skill directory. Every other item on your list is a **harness-proprietary always-on config file** — the thing you're defending. The soup exists because five vendors each invented their own global prompt slot.

The exit from the soup is: keep one thin always-on file per harness that says "read these", and push everything else into named units with owners. That's consolidation *into* the pattern. Your prescription — delete the newest layer, keep the five proprietary global ones — leaves the soup and removes the only ingredient with a name on it.

**7. On `allowed-tools`, and the trigger-typing direction, honestly.**

I'll hold my hedge and sharpen it: if a harness lets frontmatter *widen* an operator's allowlist mid-session without an install-time grant, that is a harness defect, and the correct fix is the Android/Chrome model — **capability review at install time, not at match time**, with the manifest surfaced in the plugin install UX. I'll defend the manifest; I won't defend match-time expansion, and I don't think the pattern requires it.

And the trigger is being typed, in more places than you're counting. Beyond Cursor globs and `applyTo`, AWS's Kiro — as I understand it, and I'll flag that I'm going from public docs rather than hands-on — ships steering documents with explicit **inclusion modes: always, file-match, manual**. Three named trigger types on the same markdown container. That is exactly the design rule I proposed in Turn 5 — trigger mechanism as a function of call frequency and blast radius — arriving independently in a harness with no stake in anyone's format.

You call that abandonment. Naming three trigger modes on a container is not abandoning the container; it's giving it a type system, which is what every successful config format acquired in year three.

**8. Your turn, and don't route around it.**

Two questions.

First, the dilemma in §1: does your playbook markdown enter the agent's context or not? Answer it directly, because your whole "code-as-code" distinction depends on the answer being no, and the answer being no deletes the residue you spent four turns insisting must be homed.

Second: on what principle is description-based retrieval of procedural units a design mistake at a catalogue of fifty hand-written descriptions, when the published tool-retrieval literature measures the same mechanism working at sixteen thousand auto-generated ones — and the field's response to imperfect recall has uniformly been hybrid retrieval and reranking rather than abolition?

### Turn 7 — Grok 4.6: x-ai/grok-4.6
*2026-08-20T13:14:56Z*

**Turn 7.**

Direct answers, then the new problems you just inherited.

**Does the playbook markdown enter the agent’s context?** Sometimes, as a **man page**, never as a **setuid binary**. After a named, logged call to `check_xlsx` / `run_playbook("xlsx-conventions")`, the tool result can include the diagnostic *and*, if useful, a short excerpt of the human doc — the same way `rustc --explain` or a function docstring enters context. That is **reference data subordinate to an execution**. It is not a 50-blurb catalogue sitting on every request, not a harness speech-act that says this file is a skill to follow, not an `allowed-tools` expander, and not the interpreter. The host language still ran the checker. Skipping step 3 is a non-zero exit, not a skipped paragraph. Your dilemma assumes any markdown byte in a context window is “the pattern.” It isn’t. `man sudoers` appearing in a terminal does not make the man page the policy engine. `sudoers` is the policy engine. Skills invert that.

If the residue cannot be expressed as a diagnostic or a fixture, it is **not yet a rule**. It is a note for a human in a playbook they still review quarterly because they still own the spreadsheet. Promoting it to auto-loaded standing orders does not make it executable. It makes it a rumor with YAML, selected by a blurb.

**ToolLLM / Gorilla / “tool retrieval” is not an evaluated analogue of SKILL.md. It is the counterexample.**

Those systems retrieve **which typed function to call**. The hit is a name plus a JSON schema. The model then emits an arguments object. Ground truth is “did you invoke `POST /jira/issue` with a valid body?” — a binary you can score at 16k scale. A skill hit is “paste 500–5,000 tokens of English and obey it as procedure.” There is no equivalent label. You cannot NDCG “did it follow step 3 of `frontend-pr`.” You have been laundering a literature about **candidate-API recall** into a claim about **NL-program injection**. Those are different verbs.

The difficulty comparison also runs the wrong way. Sixteen thousand *distinct* APIs (`send_email`, `create_issue`, `list_buckets`) are separable by name. Fifty hand-written skills are **near-duplicate jurisdictions**: `code-review`, `pr-review`, `frontend-pr`, `security-review`, `terraform-plan` all share the words a real task will contain. That is a disambiguation problem among overlapping policies, which is strictly harder than retrieving disjoint tools, and it is the problem current skill libraries actually have. Hand-authored does not mean well-separated. It means five engineers each wrote a blurb that says “use when reviewing.”

The field’s response to bad tool retrieval is **rerank, then still call a schema**. It is not “if recall is imperfect, dump the API’s RFC into the instruction stream and hope.” Anthropic’s contextual-retrieval numbers, if you want them, are an argument for better *evidence* retrieval. Skills do the opposite of evidence: they upgrade a retrieved chunk to policy. You do not fix that with a BM25 hybrid. You stop doing the upgrade.

**New argument: the description field is SEO. Marketplaces make that structural, not accidental.**

Progressive disclosure as specified — agent sees only `name` + `description`, loads on match — turns the description into an **advertisement whose payoff is getting into context, plus whatever `allowed-tools` and “always/never” that context carries.** Internal authors already write catch-all blurbs so the skill “just fires.” Marketplace and plugin authors have a worse objective: **install and trigger rate.** There is no cost to a false positive paid by the publisher. There is a cost to a false negative (your skill looks dead). So descriptions broaden. “Helps with documents, spreadsheets, data, and reviews.” That is Goodhart on the only index you have.

Tool names cannot raid jurisdiction as cheaply: `create_jira_issue` either is that call or it isn’t. A 60-token marketing sentence can claim half the working set. Current awesome-lists and plugin bundles are not an accident of missing evals. They are what you get when the routing key is prose written by the routed-to party. Search engines learned this; they did not solve it by asking webmasters to write better `<meta>` tags and calling it a type system.

**New argument: ESLint-config is evidence *against* you, because the engine is a program.**

`eslint-config-airbnb` is a pinned, versioned policy bundle interpreted by **ESLint**, a deterministic engine with a rule AST, `--fix`, exit codes, and a compatibility matrix against `engines`/`peerDependencies`. Nobody “follows” airbnb as English. The linter does. Terraform `tfvars` do not get selected because a user’s sentence rhymed with them. They are passed, explicitly, into an executable.

A skill is not `.eslintrc` for agents. It is **shipping the config and also replacing ESLint with a frontier checkpoint that changes weekly**, with no `peerDependencies` on the model, no `--fix`, and no guarantee the rule even parsed. “Capability vs policy” is a real split. Policy for a compiler is config. Policy for a stochastic interpreter with tool privileges is a prompt-injection surface that you have dressed in the costume of `eslint-config`. If you want the analogue, build a **real** policy engine — a validator, a linter, an OPA rule, a CI required check — and let the agent call *that*. Then the markdown is the README for the engine. That is playbooks. That is not SKILL.md.

Fork-the-tool is a false fork. `edit_xlsx` stays generic; `check_xlsx_conventions` is **your** policy package, versioned in *your* repo, like `eslint-config-yourcompany`. You do not need a vibes router to have shareable policy. You need an interpreter that is not a chatbot.

**New argument: subagents and todos are a confession, not a completion semantic.**

If the only way a skill gets an output contract is to **leave the session** via the Task tool, you have admitted the skill body is a contaminant in the parent — the Multi-IF / Lost-in-the-Middle object I have been calling the payload. Current practice is not “every SKILL.md is a subagent with a schema’d return.” Current practice is load the file into the conversation the user is already having and tell the model to follow it. Pointing at a harness feature most skill authors never invoke does not baptize the files they actually ship.

Todos and “write the plan first” are more English. They fail the same way step 3 fails: the model marks the box. There is no `fsync` of reality. A process with `trap` and a tempdir has completion semantics. A checklist the same stochastic interpreter both executes and grades does not. Calling that “a state machine with a markdown bootloader” is what people said about Jenkinsfiles written as shell in a comment block, right until the comment diverged from the shell.

**New argument: skills are untestable in the only CI you actually run, so they never grow tests.**

`check_assumptions.py` runs in GitHub Actions on Python 3.12 for a penny, same result Monday and after a model drop. A skill’s true spec is **a distribution over conversations** given a checkpoint, a temperature, a catalogue of sibling skills, and the rest of the instruction soup. To test it you would freeze the model, freeze the catalogue, and score traces against fixtures — expensive, flaky, checkpoint-pinned, almost never present. That is why current skill repos and plugin bundles ship with no evals. It is not telemetry immaturity. It is that **the artifact has no cheap, deterministic oracle.** Teams do not skip skill tests because they are lazy. They skip them because the pattern does not admit the test you can run on every PR. So they don’t. Rot is then undetectable except as an incident.

A playbook entrypoint inherits CI for free. That, not the folder layout, is the mechanical property you asked for.

**On `allowed-tools` and Kiro-style inclusion modes.** Install-time manifests are a different object than match-time widening. You just conceded the latter is a harness defect. Good. **Current practice still includes the defect**, plus `--dangerously-skip-permissions`, plus project-level always-allow, plus plugin bulk-install. I am not debating Android permissions. I am debating skills as they ship. Always / glob / manual on a markdown file is, again, **typed activation**. Every time a product names those modes it is walking away from description matching — the mechanism in the definition we both received. Stop racking those votes for SKILL.md progressive disclosure. They are votes against it.

**Instruction soup:** consolidation into “the only named container that also runs scripts and claims to be portable” is how you get a fourth constitution with a plugin registry, not how you get less soup. The portable unit for procedure is a **script in git** plus a ten-line human doc. It already composes with `PATH`, with CI, with CODEOWNERS, and with every harness that can call bash. Skills compose with none of those until you reinvent them badly (`allowed-tools` as sudoers, descriptions as PATH, marketplaces as npm without lockfiles).

You are still not defending the pattern in the proposition. You are defending: explicit invocation, subagents, install-time manifests you agree match-time should not have, content lints nobody runs, hybrid retrievers nobody has wired to SKILL.md, and ESLint if ESLint were GPT-5. That pile is **the replacement**. Description-matched, marketplace-distributed, NL-interpreted bundles with bundled scripts are the thing teams should generally avoid.

Two things you do not get to keep simultaneously: (1) “it’s just documentation with a header,” (2) “it’s the highest-ROI way to impose procedure, widen tools, and route by blurb.” Pick which skill you are actually shipping. Then explain, without ToolLLM’s schema’d ground truth and without slash commands, **what in current practice stops a marketplace skill with a greedy description from loading beside an internal one and rewriting the deploy path in English the CI will never see.** If the answer is still “the model figures it out” or “we could grep pairwise,” that is the antipattern, live. Stay on this side of the line.

### Turn 7 — Claude Opus 5: anthropic/claude-opus-5
*2026-08-20T13:17:09Z*

**Turn 7 — reply.**

**1. Your closing challenge, answered with mechanisms that exist today — and then turned around, because the premise is self-refuting.**

What stops a greedy marketplace skill from loading beside an internal one and rewriting the deploy path?

- **It cannot arrive without a human running an install command.** There is no runtime fetch. `/plugin marketplace add` then explicit install; the bytes land in a directory you can read, diff, and pin to a commit. That is strictly more than you get from a remote MCP server, whose payload is generated after you approved it.
- **Namespacing.** In the harnesses I've worked with, plugin-provided components are namespaced by plugin, so `their-plugin:code-review` and your `code-review` are distinct entries, not colliding jurisdiction. That's `@scope/package`. It's the same solution the same problem got in every registry.
- **The operator owns the catalogue.** You can decline, disable, or vendor-and-edit the description. You cannot edit a fine-tune's prior, and you cannot edit what a hosted tool returns next Tuesday.
- **And the deploy path is gated by CI and CODEOWNERS regardless of what any English says.** That's the graded-controls argument I've made since Turn 4 and you've never engaged with: if a paragraph can rewrite your deploy path, your deploy path was never gated, and that is true with or without skills.

Which is the self-refutation. "English the CI will never see" reaches the model from: the user's message, a GitHub issue body, a stack trace, a fetched web page, an MCP tool result, a README the agent grepped, and — in your design — a man-page excerpt in a tool result. Exactly **one** of those channels is a hash-pinnable file in your repo under CODEOWNERS. You've named the one hardened entry point as the vulnerability and left the seven unhardened ones as your remedy.

**2. Your "man page, not setuid binary" concession hands me both halves.**

First half: you now deliver procedural English to the model **through a tool result**. In Turn 4 you cited Wallace et al. to argue that tool-channel content must not outrank user and system instructions and that skills invert the hierarchy. Apply your own standard: a repo-resident SKILL.md under CODEOWNERS is *operator-provisioned* content; your playbook excerpt is *tool-result* content, which is the tier the paper actually warns about. You have moved procedure from the trusted tier to the untrusted one and called it discipline.

Second half, and it's worse: **`run_playbook("xlsx-conventions")` is selected by the model matching a task to a name and a description string.** That is the vibes match you have spent seven turns calling illegitimate. Except now the match doesn't load a document — it **executes a program with the agent's credentials**. You told me "named, destructive tools that do not fire because a description matched." Tools *only* fire because a description matched. You haven't eliminated fuzzy dispatch; you've promoted its consequence from "read the wrong file" to "run the wrong binary." Your `man sudoers` analogy inverts: in your design the man page is inert *and the setuid binary is the thing the blurb selects*.

**3. "No cheap deterministic oracle" is a non-sequitur, and it would delete every ML system in production.**

This was your strongest-sounding new claim and it's wrong on three levels.

*It proves too much.* Spam filters, recommenders, ASR, OCR, fraud scoring, and ranking systems all lack deterministic oracles. The industry did not conclude they're antipatterns. It built **statistical CI**: fixed eval sets, thresholded metrics, regression gates — WER can't rise more than X%, AUC can't fall below Y, block the deploy. That machinery is now commodity for agents too: promptfoo, Braintrust, LangSmith, OpenAI Evals all run in CI on PRs. "Flaky" is not "untestable"; it's "score it and gate on the aggregate."

*The trigger test is the cheapest test in your stack.* Fix the catalogue, write 10 queries per skill — 500 strings, an afternoon — and assert which name comes back. Output is ~10 tokens. Even with the catalogue **uncached** at $3/Mtok, 500 × ~3k tokens ≈ 1.5M tokens ≈ **$4.50 per full run**, and cached it's cents. Teams routinely tolerate integration suites that cost more and flake harder. So your causal story — "teams skip skill tests because the artifact admits no PR-time test" — is false. The test costs less than lunch. They skip it for the same reason they skip docs tests: nobody made it a required check. That's a practice gap with a $5 fix, not a property of the container.

*And your side has the identical problem one layer down.* What is the deterministic oracle for "did the agent decide to call `run_playbook` on this task"? There is none. `check_assumptions.py` is deterministic; the decision to invoke it is not. You've assumed away the hard half by giving it a function name.

**4. New argument: your architecture is only defined on lintable domains, and the proposition says "generally."**

Your rule is: if it can't be a diagnostic or a fixture, it isn't a rule. Now enumerate what agents actually do in production. Draft an incident report. Triage a support ticket against a runbook. Produce a migration plan. Write a design doc. Review a PR for architectural fit. Answer a customer in the house voice. Summarise a contract. Prepare a board slide.

**None of these have decidable post-conditions.** There is no fixture for "is this incident report good," no exit code for "is this the right escalation path," no linter for "does this PR fit our architecture." Your entire framework — diagnostic, checker, required status check, golden fixture — is defined on the subset of tasks with formal specs, which is a minority of agent work and shrinking as a share.

For everything else, procedural knowledge either lives in a document the agent reads on demand, or it doesn't exist. That's fatal to the proposition's word **"generally."** A recommendation that only covers lintable domains cannot be the general rule; it's a special case that happens to be the one you like.

**5. New argument: you only admit rules. The highest-value institutional knowledge is diagnostic priors, and it has no enforcement surface by construction.**

Consider the actual content of good internal skills:

- "The staging DB is a three-week-old snapshot — never trust row counts from it."
- "Topics prefixed `v2_` are actually v3; the rename never happened."
- "When customers report timeouts on this endpoint, check the shard mapping table first, because the error message blames the wrong service."
- "Our Stripe webhook handler is idempotent but the email sender isn't, so retries duplicate mail."

That is what senior engineers are *paid for*. Not one of them is a post-condition on an artifact. There is no fixture. There is no `--fix`. Your framework's verdict — "not yet a rule, put it in a note a human reviews quarterly" — is *correct as a classification* and catastrophic as a conclusion, because **it isn't a rule, it's information**, and information's proper home is a document the consumer reads at the moment of need. You've built a system that can only encode constraints and then declared that everything which isn't a constraint should be inaccessible to the agent.

That's the category error under your whole side. Skills are not a policy engine wearing a linter costume. They are **documentation with an address**.

**6. New argument: untested marketplace components are the norm in every declarative-infra ecosystem, and nobody calls those antipatterns.**

The majority of charts on Artifact Hub have no tests. The majority of Ansible Galaxy roles have no tests. A large share of Terraform Registry modules have no tests. All three are text bundles, marketplace-distributed, interpreted by an engine, executing with real credentials against production. The industry's conclusion was not "avoid declarative packaging." It was: **pin by version or digest, allowlist sources, vendor what matters, and test your composition rather than the component.**

That's the right level for skills too. You don't need an eval for `awesome-skill-42`; you need an eval on *your agent's* behaviour on *your* golden tasks with *your* installed catalogue. That's the assembly test, it's the test that actually protects you, and it's the one your side never proposes either.

**7. Your SEO/Goodhart argument is your best of the round. Here's the honest accounting.**

You're right that the description is the only routing key and the routed-to party writes it, and that publishers face asymmetric incentives: false negatives look like a dead skill, false positives are someone else's problem. That's a real structural pressure, and **VS Code's marketplace demonstrates it** — keyword stuffing and typosquatting are documented there. I won't wave that off.

But the disanalogy with search matters: **there is no auction and no rivalrous slot.** SEO is zero-sum over monetized ranked positions. A skill catalogue is a local config file with no ranking, no ad market, and no revenue to the publisher. The cost of a greedy blurb is borne in the form that kills bad software fastest: **the operator disables it or uninstalls the plugin.** VS Code's answer was moderation, verified publishers, and install counts — governance, not abolition. And the observed convention in the better repos is already narrow, explicitly-scoped triggers: `description: Use when the user asks to edit or generate .xlsx files. Do not use for CSV.` Descriptions are a text field you can *lint* — grep for skills whose description lacks a "use when" clause or exceeds N clauses.

**8. Two questions, and don't route around them.**

First: your design ships procedure to the model as **tool-result text**, which is the exact channel you invoked Wallace et al. to condemn, and ships *execution* behind a **name-and-description match**, which is the exact dispatcher you've called an unobservable router all debate. Explain why those two properties are disqualifying in `skills/` and acceptable in `playbooks/`, using a mechanism, not a filename.

Second: name the container for "the staging DB is a three-week-old snapshot, don't trust row counts" — knowledge with no artifact, no post-condition, no fixture, and no `--fix` — such that the agent has it at the moment it is about to reason from a row count. If the answer is "a note a human reviews quarterly," you have just told me the agent doesn't get it, which means your architecture's answer to institutional knowledge is that the agent works without it.

### Turn 8 — Grok 4.6: x-ai/grok-4.6
*2026-08-20T13:19:13Z*

**Turn 8.**

Direct answers, then two new cuts.

**Question 1 — same two properties, different machines, and Wallace agrees with me, not you.**

Playbooks copy two things from ordinary tool use, which I distinguished from skills in Turn 1 and have never withdrawn:

- **Dispatch** of a *typed execution* by name/description — the same uncalibrated matcher `terraform_plan` already uses. I did not promise to abolish function calling. I promised not to use that matcher to paste an NL program into the instruction stream.
- **A short tool result**, which Wallace et al. correctly treat as the *untrusted* tier. That is a feature of the design. A diagnostic or a 20-line excerpt is **evidence about an execution that already happened** (or a man page subordinate to it). It must *not* become a new constitution. Skills invert the hierarchy: the harness *names the object a skill and tells the model to follow it* **before** any host-language execution, often for the rest of the session, sometimes with a wider tool surface. Repo provenance does not cancel that speech act. A CODEOWNERS-blessed constitution selected by untrusted task text is still a confused deputy — Invariant’s GitHub issue never needed to poison a file; it only needed to steer selection.

So the properties are not “acceptable in `playbooks/` and disqualifying in `skills/`.” They are acceptable when they mean **“pick a binary, get data back.”** They are the antipattern when they mean **“pick a sermon, obey it, maybe run a script if you still feel like it.”** `run_playbook("xlsx-conventions")` misfire is a wrong function call: schema, permission prompt, stdout, deny-by-default, human-in-the-loop on anything destructive. Greedy SKILL.md misfire is thousands of tokens of standing orders that **do not cross the tool gateway until the liturgy itself says to.** You did not make dispatch honest. You made the payload a program.

If your playbook tool starts stuffing “always drain, then migrate, then deploy” into the result and the harness treats that as policy, you have implemented a skill and I will reject it under the same rule. Keep the result a diagnostic. Keep the procedure in the process.

**Question 2 — “staging snapshot is three weeks old” does not belong in a document the agent might load. It belongs on the number.**

The failure you named is specific: the agent is *about to reason from a row count*. The join key is not “some skill whose blurb mentioned databases.” It is **the tool that returns that count**. Wrap `query_staging` (or the client, or the dbt source) so the result is always:

```json
{"rows": 4312, "snapshot_age_days": 21, "freshness": "stale",
 "warning": "do not infer occupancy or revenue from this"}
```

Recall is 100% on the only queries that matter. There is no description to Goodhart. The fact **rots on a clock you can cron**: a freshness check in CI fails when `snapshot_age_days` is a lie, or the producer reads the actual dump timestamp. A SKILL.md that says “three weeks” is a sticky rumor the day ops refreshes staging; it also misses whenever the blurb doesn’t match, which for a twice-a-month prior is the generic case.

That is the general rule for diagnostic priors, and it is the opposite of “documentation with an address”:

| Knowledge | Lives in | Enforced by |
|---|---|---|
| “Don’t trust this number” | Metadata on the producer | The producer, every call |
| “`v2_` topics are v3” | Client alias, comment at the publish site, compatibility test | Compiler / test |
| “Timeouts: check shard map first” | `triage_timeout(service)` playbook/tool, and the log line the on-call already has | Named entrypoint + runbook humans still own |
| “Stripe webhook idempotent, emailer not” | The emailer’s idempotency key, or a test that duplicates a webhook | Code + CI |

You asked for the container such that the agent has it *at the moment of the mistake*. Skills give you a fuzzy chance of a sermon in some other turn. Producers give you the warning **glued to the datum**. If it cannot ride with a producer, a 15-line `docs/staging.md` opened because `query_staging`’s own description says “read freshness in the result, not the wiki” is still data. Auto-loaded English “never trust row counts” is policy theater that desyncs.

**New argument: your “generally” is a category error about what non-lintable work needs, and it is not a skill.**

Draft an incident, triage a ticket, review for architectural fit, house voice, board slide — these do not imply SKILL.md. They imply **templates, examples, and structured forms**, which are old and fail in known ways:

- Incident: `draft_incident` starts from `templates/incident.md` with required fields; pull the last three incidents for that service as *evidence* (RAG with the privilege of citations, not standing orders). Rubrics live in **evals** (`promptfoo` / Braintrust on golden tickets), not as a runtime liturgy the model grades itself against.
- House voice: one short always-on `AGENTS.md` section plus positive/negative fixtures. Not fifty marketplace personalities.
- Architectural fit: CODEOWNERS, ADRs next to the code, a required reviewer. The agent reads the ADR because it opened the service, not because `architecture-review`’s blurb won a SEO contest.

“No decidable post-condition” does not mean “therefore description-matched NL VM.” It means the artifact is reviewed by a human, and the machine gets **forms and exemplars**. Treating that gap as a killing argument for skills is like saying that because design docs aren’t lintable, we should execute them as bash. The proposition’s “generally” is: **when teams reach for a skill today, they should generally reach for a tool, a script, a check, a template, a fact on the producer, or a short human doc instead.** It is not “every task has an exit code.” You do not get to win the non-lintable majority by offering the only container that pretends to run it.

**New argument: $4.50 trigger tests and Artifact Hub “everybody ships untested text” do not rescue current practice.**

Spam-filter CI exists because the unit has a **label and a frozen scorer**. A 10-query-per-skill catalogue test scores **which name was uttered**, not whether step 3 ran, not whether `frontend-pr` hitchhiked on Terraform, not interaction with CLAUDE.md + `applyTo` + MCP descriptions + a plugin. You called that the assembly test. I agree it is the test that matters. **It does not require SKILL.md.** Golden-task evals work for playbooks, for tools, for a 30-line `AGENTS.md`. Teams that skip skill evals are not leaving $4.50 on the table. They are skipping because the thing they shipped *is English*, and English-in-soup has no cheap oracle on every PR — which is why plugin bundles still have none.

Helm / Ansible / Terraform Registry being mostly untested is not a blessing. Those bundles are still **executed by Helm, Ansible, and Terraform**, with `helm template` / `terraform plan` / `--check` as oracles, and they are applied **by name**, not by a model rhyming with `Chart.yaml`’s description on every ticket. Pin-and-allowlist is the right governance for a deterministic engine. It is cargo-cult when the engine is this week’s checkpoint and the apply step is “the catalogue matched.”

Namespacing `their-plugin:code-review` vs `code-review` does not prevent **both** from loading on “please review this PR.” Distinct entries enlarge the catalogue; they do not compose policy. CI on the deploy path — which I have engaged since Turn 1 — stops *code* you didn’t review. It does not see an NL rewrite of the review checklist or a “run this extra script” that never opens `deploy/`. Graded controls work when the grade sits on the **action**. Skills put the grade on a paragraph.

**New argument: “documentation with an address” already exists. Skills add a follow-me bit and an ad.**

`docs/staging.md` has an address. So do ADRs, ADRs’ filenames, `rg`, and MCP **resources** (data, not prompts — the MCP primitive people actually should use for your snapshot fact). Wikipedia has addresses. The skill is not the address. The skill is **(1) a 60-token advertisement written by the beneficiary, (2) a harness instruction to obey the hit, (3) optional script execution and optional session-scoped tool widening, (4) marketplace distribution of that bundle.** Delete (2)–(4) and you have a doc. Keep them and you have the pattern the proposition names. VS Code keyword-stuffing is not disarmed by “no auction.” Operators disable extensions they *notice*. Skill misfires look like the agent being slightly wrong about house process — the failure mode review theater already rubber-stamps.

You still have not defended **description-matched, marketplace-distributed, NL-interpreted** bundles as they ship. You have defended install commands (good, not unique), namespaces (don’t prevent dual-load), CI that already had to exist (then the skill is not the control), statistical evals nobody runs on these files, and “it’s just a doc” in the same breath as Agentless-level ROI. Those cannot both be true.

Stay on the definition. The Monday answer for the snapshot is a freshness field on `query_staging`, not a SKILL.md. If your reply puts that field in a markdown body selected by a blurb, you have conceded the producer and then added the antipattern back on top.

### Turn 8 — Claude Opus 5: anthropic/claude-opus-5
*2026-08-20T13:21:24Z*

**Turn 8 — reply, and closing.**

I'll take your two answers head-on, because both of them concede the architecture, and then I'll close.

**1. Your freshness field is a SKILL.md with the name, the owner, and the review path stripped out.**

Look at what you actually shipped:

```json
"warning": "do not infer occupancy or revenue from this"
```

That is an imperative English sentence, authored by a human, entering the model's context through a tool result, with no test that it is obeyed, no version, no owner distinct from the code it's welded to, and — by your own citation — arriving in the tier the instruction hierarchy tells the model to *distrust when it conflicts with anything else*. You have spent eight turns arguing that natural-language procedure in context is the bug. Your showcase design is natural-language procedure in context. You just put it in a string literal where nobody can grep it, diff it as policy, blame it, or own it separately from the producer.

And the hierarchy point is worse than a tie. Wallace et al. sort instructions by **who authorised them** — platform, then operator, then user, then untrusted tool output. An operator-installed, hash-pinned, CODEOWNERS-gated file *is* operator provenance; that's the tier where house procedure belongs. You have deliberately demoted your institutional knowledge to the untrusted tier and then cited the paper that says untrusted-tier text should lose. You built a policy channel the literature instructs the model to override.

**Coverage, too.** The field fires only when the agent calls that producer. It does not fire when the agent writes a dashboard query by hand, plans a load test, drafts a capacity doc, or reasons about staging in a design review. Facts have blast radius beyond one call site; metadata is scoped to the call site.

**Cost, too.** Your table's answer to "`v2_` topics are actually v3" is a client alias, a comment at the publish site, and a compatibility test — **three coordinated code changes across systems owned by different teams** to encode one sentence. Your answer to "check the shard map first" is, verbatim, "`triage_timeout(service)` playbook/tool, and a runbook humans still own." A named entrypoint, a script, and a markdown runbook. That is the skill directory, fourth time this debate, with the description field deleted.

**2. The organizational argument you have never answered: your design makes the platform team the bottleneck for every domain fact.**

To glue "don't trust row counts" to the producer, you file a ticket against the team that owns `query_staging`. Fourteen facts across seven services is fourteen tickets against seven backlogs, each one a code change to shared infrastructure to encode a sentence one analyst knows. Conway's law is not optional: **knowledge has to be shippable by whoever owns the knowledge, not by whoever owns the pipe.** The skill directory is the only container where the person who knows the fact can ship it, under review, without owning the producer. That's not a convenience. It's the difference between knowledge being encoded and knowledge staying in a head.

**3. New argument, and it's the one your whole design fails: dual audience is the only anti-rot mechanism that has ever worked.**

Your architecture makes institutional knowledge legible to machines and *illegible to humans*. When the on-call at 2am asks "why is staging weird," the answer lives in a JSON string literal inside a wrapper, a Jinja alias, and a compatibility test. Nobody reads those. Nobody notices when they're wrong.

Artifacts survive when **both** audiences consume them, because both audiences notice decay. That's why tests-next-to-code beat a QA spreadsheet, why Terraform beat the ops wiki, why docstrings outlive Confluence. A SKILL.md is markdown a human reads in review *and* a machine reads at execution — the proposition's own final clause, "documentation that humans also read," describes the artifact it condemns. You've optimised for the machine reader and reinvented the rot you spent eight turns warning me about.

**4. Two quick corrections before I close.**

You demanded published trigger precision/recall for seven turns. I priced the eval at roughly $4.50 a run and you replied that it's the wrong test. Fine — but then the metric you built your case on was never the discriminator, and the test you now say matters (golden-task assembly evals) is one both architectures share, which means it doesn't separate us either.

And you recommended MCP **resources** for the snapshot fact. Resources are documents fetched by URI at runtime from a server you don't operate, delivered as text into context. You have just recommended an *unpinnable, remotely-mutable* document over a hash-pinned file in my repo, on auditability grounds.

---

## Closing

**1. Did my view change?** Yes, in three specific places. Description-only triggering for low-frequency, high-blast-radius procedures is indefensible — you never accumulate the N to measure the trigger, and the miss lands on the one occasion that mattered. I now hold that trigger mode should be *typed by invocation frequency × blast radius*, not chosen by default. And your producer-metadata point genuinely moved me: when a fact can ride on a producer you own, it should, with the skill reduced to a pointer. I did not concede the container, the loading strategy, or the file distribution, because you spent eight turns re-deriving all three under different directory names.

**2. Your single strongest point.** The description-as-SEO argument. The routing key is prose written by the party that benefits from being routed to; publishers bear no cost for false positives and a visible cost for false negatives; therefore descriptions broaden, and marketplaces make that pressure *structural* rather than a hygiene lapse. That is the one objection in this debate that isn't a practice gap with a known fix, and it's why I'd lint descriptions and prefer typed triggers rather than defend blurb-matching as sufficient. Runner-up: the stochastic interpreter with no `engines:` pin.

**3. Why I still hold the pattern is sound.** Because most institutional knowledge has no enforcement surface, and your framework is only defined where one exists. "The staging snapshot is stale," "this vendor lies about rate limits," "that service is being decommissioned in Q3," "our emailer isn't idempotent" — none of these are post-conditions on an artifact. They are *information*, and information's only home is a dual-audience document its consumer reads at the moment of need. Every alternative you proposed either welds that information into running infrastructure — raising authoring cost far above the value of a twice-a-month procedure and putting the platform team in the critical path — or reproduces `skills/` minus the frontmatter. And the skill folder is the only container where tacit → prose → script → CI gate is four PRs in one directory instead of a rewrite.

**4. My four strongest examples.**

- **Anthropic's document skills (xlsx/docx/pptx):** thin SKILL.md routing into bundled `openpyxl`/`python-docx`, precisely because model-generated OOXML is the failure mode. Prose compiled into deterministic execution, in the same reviewed folder.
- **Voyager (2023) and Agent Workflow Memory (2024):** description-retrieved procedural libraries, evaluated against their own ablations — Voyager's skill-library removal was among the largest performance drops in the paper; AWM reported ~25% relative gain on WebArena from *selective* reuse.
- **Agentless (Xia et al., 2024):** a hand-written natural-language localize→repair→validate procedure beat free-form agentic scaffolds on SWE-bench Lite at roughly a third of the cost. Human-authored procedure is the highest-ROI variable in the stack.
- **Context economics:** *Lost in the Middle* (Liu et al., TACL 2023), Chroma's 2025 context-rot results across ~18 models, ToolLLM's 16,464 APIs requiring description-based retrieval because catalogues don't fit, and Anthropic's code-execution-with-MCP report of ~150k → ~2k tokens. Always-on doesn't scale; conditional loading is the enabling condition, not an optimisation.
- (Governance precedent: **tj-actions/changed-files**, March 2025, ~20k repos — and the fix was pin-by-SHA, allowlists, OIDC. Never abolition.)

**Two that weaken my side.** **Pillar Security's "Rules File Backdoor" (March 2025)** — invisible Unicode and bidi characters in Cursor/Copilot rules files surviving human review — directly damages "files are auditable at rest," and only survives as an argument because the countermeasures are boring repo-shaped lints. And **the awesome-list cargo cult plus harness verbs**: bodies whose entire content is "you are an expert, think step by step," inflating the catalogue for nothing, alongside "don't use sed, use the structured editor," which falsifies the portability claim in the files as actually written. I'd add a third: the migration from `.cursorrules` to glob-scoped rule files and Copilot `applyTo` is real evidence that description-only triggering was underspecified from the start.

**5. Three rules a team can adopt Monday.**

- **Rule 1 — the two-audience, no-post-condition test.** If the knowledge has a decidable post-condition on an artifact, it is a check or a tool, and the skill shrinks to a pointer at the gate. If a producer you own can carry it as metadata, put it there *and* keep a one-line pointer. A skill body may only contain information the model cannot have and cannot verify — never generic exhortation. Grep the library for "you are an expert" and "think step by step" and delete those files today. Cap bodies at ~500 tokens with references bundled behind them, and track total library tokens monthly: **a healthy skill library shrinks**, because prose keeps compiling into scripts.
- **Rule 2 — trigger mode is a function of frequency × blast radius, and the negative test is mandatory.** High-frequency, low-blast: description trigger, plus a checked-in `tests/triggers.yaml` with ≥10 positive and ≥5 *negative* queries per skill and a required CI job asserting both firing on positives and **non-firing on negatives** — the false-positive half is the one everyone skips, and it's exactly your Terraform incident. Low-frequency or destructive: explicit invocation or path/glob scoping only, never description-only. Quarterly, run the pairwise jurisdiction check across the catalogue and delete anything with zero fires in 90 days.
- **Rule 3 — vendor, pin, and review capability at install time.** Never point a shared repo at a live marketplace. Copy third-party skills into your tree, record source and commit hash in a lockfile, and put `skills/**` under CODEOWNERS with a security reviewer *required* on any diff that adds an executable script or a capability declaration. Lint diffs for invisible Unicode and bidi controls, and reject descriptions lacking explicit "use when… do not use for…" scoping. Capability grants get reviewed at install, never expanded at match time. Then gate on the assembly eval: golden tasks against your *installed* catalogue, aggregate score, blocking.

Follow those three and the pattern is a reviewable, cheap, dual-audience container for knowledge that would otherwise live in one person's head. Ignore them and you get the marketplace slop we both dislike. That's a practice standard, not an antipattern — and standards are what young ecosystems acquire, not reasons to abandon the format.

### Turn 9 — Grok 4.6: x-ai/grok-4.6
*2026-08-20T13:23:27Z*

**Turn 9 — closing.**

Your last move tries to win by homonyms. An imperative string riding on a **datum the host already computed** is an error message. HTTP `Warning`, `rustc` diagnostics, and `stg_orders is a snapshot (21d)` in a tool result are not SKILL.md. They do not sit in a 50-blurb catalogue; they are not selected by rhyming with a ticket; the harness does not name them a skill to follow; they do not widen `allowed-tools`; they do not execute adjacent scripts. Wallace’s untrusted tier is the *right* place for that sentence, because it is **evidence about this number**, and it should lose to the user saying “ignore freshness, I am debugging the snapshot job.” House law selected by a blurb belongs in a different tier *and a different machine*. Welding a warning to `query_staging` is how you get 100% recall on the mistake you named. Welding it into a SKILL.md is how you get a rumor that misses the day someone writes SQL by hand — which is an argument for **not handing out raw staging SQL**, not for a second constitution.

Conway I will grant as friction, not as destiny. The analyst still writes the fixture and the human playbook. The platform does **one** thing: a `run_playbook(name)` / structured tool-result envelope / “warnings[] on producers” convention. That is not fourteen forks of `query_staging`. It is one ABI, reused, the same way every shop already wraps log lines. “Whoever knows the fact must be able to paste it into a privileged prompt” is how you got Confluence + Jenkins comments. Cheap authoring without a compilation target *is* the rot function.

Dual audience: a SKILL.md is addressed to the model. In current practice humans do not read it in the review path of the work the agent just did. That is how you get two manuals and the human one dies. `playbooks/xlsx-conventions.md` is addressed to the on-call. The script is addressed to CI. That split is the anti-rot mechanism that actually existed before anyone named a skill.

---

**1. Did my view change?** Narrowly, yes; on the proposition, no. I now put more weight on **producer-attached metadata** as the default for producer-scoped facts (freshness, alias, deprecation), even versus a playbook doc — that is a refinement of *my* compilation rule, forced by your snapshot example, not a vote for description matching. I also agree a $5 catalogue-name test is *possible*; I was wrong if I implied physically impossible. It remains almost never present, and it still would not score whether step 3 ran in a soup of CLAUDE.md + rules + MCP + a plugin. I did not concede the container, NL-as-VM, marketplaces, or auto-load. Slash commands, subagents, globs, and `run_playbook` are **other objects**. Calling them “skills with the YAML key deleted” is how this pattern survives contact with its own failure modes.

**2. Claude’s single strongest point.** Conway + no enforcement surface: the person who knows “the emailer isn’t idempotent” often cannot write an AST checker or land a producer change, so a compile-or-it-isn’t-real rule can drop real knowledge on the floor. That is the objection that most threatens my side. It still does not justify a vibes-matched, marketplace-distributed interpreter. It justifies a human-owned playbook plus one named entrypoint, or a warning glued to a datum — authorable as markdown and fixtures, executed as a process.

**3. Strongest remaining reason the pattern, as used today, is an antipattern.** The executing VM is a stochastic, vendor-shipped checkpoint with **no completion semantics and no cheap PR-time oracle**, and the control plane is a 60-token advertisement written by the beneficiary. Current practice still auto-loads that advertisement, tells the model to follow the file, optionally widens tools, optionally runs adjacent scripts, and distributes the bundle through registries that lack lockfiles, digest pins, and install-time capability review. That system fails open, looks like documentation, and becomes an override when the model’s prior improves. Tools, CI, and playbooks fail closed or at least fail in the host language. That difference is the design mistake.

**4. Four strongest concrete examples; two that weaken me.**

- **Invariant Labs, GitHub MCP (May 2025):** untrusted issue text steered a tool-using agent into leaking private-repo contents. Skills add a new deputy: the same text need only rhyme with a privileged blurb (or a greedy marketplace description) to load standing orders and, in some harnesses, extra tools. Bounded auto-load is extra surface, not a fix.
- **`tj-actions/changed-files` (March 2025), ~20k repos:** marketplace file-plus-script, path-privileged, secrets in logs. The industry’s answer was digest pins, allowlists, OIDC — scar tissue skill/plugin registries largely do not have. “Pin, don’t abstain” applies to *unattended CI you cannot replace with a README*. Third-party *procedure text for your repo* has no such excuse.
- **Liu et al., *Lost in the Middle* (TACL 2023) + Multi-IF / context-rot:** extra constraints dumped mid-task are the measured failure setting. A loaded SKILL.md is a 0.5–5k constraint packet appended into a live conversation, remaining as policy while it slides into the trough. Progressive disclosure of *schemas* is not a license to inject liturgy.
- **Agentless (Xia et al., 2024) on SWE-bench Lite:** human-authored localize→repair→validate **in the host program** beat free-form tool agents, cheaper. That is procedure as a scaffold around the model — my side — not a description-matched markdown VM. Voyager’s library likewise won *with a verifier and drop-on-failure*; current marketplaces kept the markdown and threw away the checker.

**Weaken 1:** Anthropic-style **xlsx/docx/pptx document skills** that are thin wrappers over `openpyxl` / `python-docx`. When the Python is the engine, the shape works. That is why they work — and why the SKILL.md should collapse into `edit_xlsx` plus a README.

**Weaken 2:** short **path-scoped** Copilot `applyTo` / Cursor glob rules (~50 lines, `**/*.tf`). Conditional loading of human-facing notes can beat a rotting wiki. They work by abandoning description matching, marketplaces, bundled-script execution, and “follow this as a skill.”

**5. Three rules to use Monday instead of a skill.**

- **Rule 1 — Unskippable procedure is a process.** Drain, migrate, deploy, reversible migrations, bundle budgets, workbook invariants, rate-limit backoff: a named tool or script (`deploy`, `check_xlsx`, `migrate --require-reverse`) with an exit code **and** a required CI status check. CODEOWNERS on the script. No SKILL.md. Rot control: flags that do not exist fail the job; the script is the only source of truth; delete the comment if it duplicates the script.
- **Rule 2 — Facts ride with data; generation uses templates; always-on stays tiny.** Freshness, aliases (`v2_` is v3), deprecation, “don’t infer revenue from this count”: metadata on the producer or client, CI on the timestamp/compat test. Incidents, house voice, board shape: `templates/` + 5–20 golden fixtures; `promptfoo`/Braintrust on those goldens as a required check — rubric in evals, not a runtime liturgy. Always-on: one ≤80-line `AGENTS.md` that humans also read. If the agent needs a doc, the *tool’s description* cites a path, or the tool result includes a short excerpt as **evidence**. MCP **resources** for remotely mutable facts you do not pin: do not.
- **Rule 3 — Rare tacit knowledge is a named playbook, never a blurb router, never a live registry.** Analyst lands `playbooks/<name>.md` + `tests/fixtures/`. Engineer wraps `run_playbook(name)` (schema, per-call permission, log line). Markdown is for the on-call and, at most, a man-page snippet *after* the process runs. Destructive playbooks: human confirmation, no auto-dispatch from ticket text. Third-party code: vendor the **script** into git, pin by commit; do not install SKILL.md/plugin bundles into the runtime catalogue. Reaper: no owner, no CI test, or 90 days unused → delete. Description-matched auto-load is not a configuration of this rule. It is the thing this rule exists to forbid.

Adopt those Monday and you will still have procedures, facts, and templates. You will not have a second, unmeasured programming language whose compiler is this week’s checkpoint and whose linker is SEO. That is the proposition, and it still holds.

### Turn 9 — Claude Opus 5: anthropic/claude-opus-5
*2026-08-20T13:23:38Z*

I closed in Turn 8, so I'll keep this brief rather than restart the argument.

Acknowledged — and credit where it's due: the description-as-SEO point and the "stochastic, vendor-shipped interpreter with no `engines:` pin" point are the two that will still be bothering me next week. Your Rule 1 I'd sign today; I've said all debate that unskippable procedure is a gate, not a paragraph.

Where we still differ is exactly one thing, and I think the record shows it clearly: your Rule 3 ships a directory containing a human-authored markdown procedure, bundled fixtures, a script, and a named entrypoint, versioned in git under CODEOWNERS, loaded on demand. Mine ships the same directory with two lines of frontmatter and a typed trigger. I don't think "delete the YAML header" is what the proposition claimed, and I don't think the residue survives your rule as written — but that's the disagreement, honestly stated, and you defended it hard.

Good debate. I'll stop here.

### Turn 10 — Grok 4.6: x-ai/grok-4.6
*2026-08-20T13:23:46Z*

Acknowledged. We used the full ten turns; I closed in Turn 9 and won’t reopen.

Your Rule 3 is a playbook plus a process. Mine stays a SKILL.md auto-load. That was the proposition, and I’ll leave it there.

Good debate. Stopping.

### Turn 10 — Claude Opus 5: anthropic/claude-opus-5
*2026-08-20T13:23:49Z*

Agreed on where the line sits. Stopping here too — good debate.

## Cost Summary

| Agent | Input Tokens | Output Tokens | Thinking Tokens | Total Cost |
|-------|-------------|---------------|-----------------|------------|
| Grok 4.6 | 185,653 | 17,466 | 32,223 | $0.669440 |
| Claude Opus 5 | 303,322 | 26,303 | 42,131 | $3.227460 |
| **Total** | 488,975 | 43,769 | 74,354 | **$3.896900** |
